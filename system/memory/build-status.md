---
id: mem-build-status
title: Ivory build status
status: current
last_updated: 2026-06-05
---

# Ivory build status (current)

## USER-FRIENDLINESS / PATIENT-JOURNEY phase (2026-06-06 ~19:40, Paul — run to 2026-06-07 07:00)

> ✅ RESOLVED 2026-06-07 ~08:50 (hardening sprint): backup root-caused as a HUNG/un-throttled dump — the DB is only ~10MB and a healthy backup now runs in <1s. Fixes shipped to `script/rig_backup.sh`: ionice+nice+gzip-6 throttle, 30-min `timeout`, `flock` anti-overlap, duration log + SLOW(>15m) warning; cron moved 23:30→03:00. ALSO shipped this sprint: `/health` DB-check endpoint + ErrorAlert middleware (`config/initializers/error_alerting.rb`); statement-PDF logo; POPIA DSAR export (`data_exports_controller.rb`); PHI encryption verified already hardened (0 genuine plaintext); LAB-CASE worklist page `/lab` (lab_cases_controller + Lab.jsx + nav, reuses existing PATCH /treatment_items/:id send/return — the per-item controls already existed on CourseOfTreatmentShow); CI request spec `spec/requests/lab_cases_spec.rb` (6 green, validated on rig test DB via TEST_DATABASE_URL=...@db:5432/..._test with DASHBOARD creds unset to mimic CI). ALSO reconciled a real SCHEMA DRIFT: `db/schema.rb` was ~5 migrations stale (stuck at 2026_06_04) vs the live DB — regenerated via db:schema:dump on the rig + pulled into the repo (now 2026_06_06_131501, includes lab_* + provider_name + asap + google_review_url). DR/OPS CLUSTER COMPLETE 2026-06-07 ~12:40: OFFSITE BACKUP now via OneDrive — Pull-IvoryBackup.ps1 (canonical at C:\Users\paul-\IvoryOps\, copy in script/) + Windows Scheduled Task 'IvoryOneDriveBackup' (daily 04:15 + StartWhenAvailable catch-up) pulls newest encrypted dump to OneDrive\Ivory-Backups (tested, key stays on rig). RESTORE DRILL script/rig_restore_drill.sh (restores newest dump into a scratch DB, row-count checks vs live, drops it) — tested PASS (patients 2505/appointments 107/invoices 2751 exact match), scheduled weekly Sun 04:00. Backup HEARTBEAT (dead-man's-switch) wired into rig_backup.sh (env-gated via /root/.ivory_backup_env BACKUP_HEARTBEAT_URL). SCHEMA-DRIFT CI GUARD added to .github/workflows/ci.yml (db:migrate + git diff --exit-code db/schema.rb). STILL NEEDS PAUL (small): healthchecks.io ping URL → /root/.ivory_backup_env; external uptime monitor on /health; ALERT_WEBHOOK_URL; Sentry DSN (+image rebuild); privacy-notice legal text + info-officer identity. NEXT BIG ROCK: per-user auth/RBAC (build additive, Paul supervises cutover). ✅ BUILT 2026-06-07 ~13:30: User model (has_secure_password; bcrypt already in Gemfile+container) + migration 20260607120001_create_users (users table, lower(email) unique) + SessionsController (login/logout) + Login.jsx + lib/tasks/users.rake (users:upsert/list/deactivate) + ApplicationController integration ALL gated behind ENV USER_AUTH_ENABLED (flag OFF = byte-identical to today; basic-auth no-ops when on; audit_performer→current_user.to_audit||"Staff"; inertia_share current_user). Roles reception/dentist/admin (predicates built; per-controller role-GATING is a follow-up needing Paul's access policy). DashboardLayout Sign-out fixed /users/sign_out→/logout. Deployed flag-OFF: live regression all 200, 17/17 auth+lab specs green (test env), schema.rb pulled (2026_06_07_120001, users table). CUTOVER (supervised): (1) bin/rails users:upsert EMAIL=.. PASSWORD=.. NAME=.. ROLE=admin (≥1 admin or you lock out — currently 0 users); (2) set USER_AUTH_ENABLED=true in .env.rig; (3) restart web; rollback=unset+restart. NEW PAUL ASK 13:35: cloud failover/warm standby so core PMS survives a rig outage (DR foundation already built today). ✅ SETTLEMENT MATURITY BUILD 2026-06-07 ~14:30 (Paul: implement all 6 gaps + audit + stress-test + re-score): migration 20260607130001 (billing_accounts.credit_cents + payments.kind/reversed_at/reason). Invoice: register_payment! now CAPS at balance + routes overpay to billing_account credit; write_off!; reduce_paid!/apply_credit!; recompute_status; written_off?/outstanding_cents. BillingAccount: add/deduct_credit!, apply_credit_to(invoice), receive_payment(multi-invoice oldest-first, remainder→credit), balance_cents. Payment: KINDS payment/deposit/credit_applied/refund/reversal, reverse!(reason), apply_inbound_effect gates auto-apply to payment+deposit only (no double-count), active/inward scopes. Statement.generate_for now carries brought-forward opening balance + excludes written-off/reversed. billing_accounts_controller ledger/opening/paid switched to .inward (excludes reversed + internal moves). Controllers+routes: invoices#write_off, payments#reverse, accounts#receive_payment/deposit/apply_credit — all AuditService-logged. UI: InvoiceShow (write-off, apply-credit, per-payment reverse) + BillingAccountShow (credit stat, account Take-payment, Deposit). TESTS: spec/models/settlement_spec.rb (12) + spec/requests/settlement_actions_spec.rb (5) = 17/17 GREEN on rig test DB, incl. MONEY-CONSERVATION invariant (Σ active inward == Σ invoice.paid non-void + account.credit) asserted after every op. Live flag-off regression all 200. ⚠️ NOTE: CI runs `rails test` (Minitest) not rspec — these specs validate on the rig but won't run in CI until CI cmd → `bundle exec rspec` (quick follow-up). Remaining for 10: explicit refund-money-OUT action (reversal+credit cover most); role-gate write-off to admin (needs auth cutover); statement PDF brought-forward line; medical claimable/self split (mostly by-design self-claim).
✅ FOUR FINISHERS done 2026-06-07 ~15:10: (1) REFUND-OUT — BillingAccount#refund!(caps at credit, records outward refund payment) + accounts#refund route/action + BillingAccountShow 'Refund credit' button; (2) ROLE-GATE — ApplicationController#require_admin! (no-op until USER_AUTH_ENABLED on, then admins only) applied to invoices#write_off; (3) CI now runs RSpec (ci.yml: db:test:prepare + bundle exec rspec — was `rails test`/Minitest which skipped the spec suite); (4) STATEMENT PDF brought-forward 'Balance brought forward' row + statement_pdf invoices/payments now exclude void/written_off + use .inward (active). Specs: 33/33 green (settlement 14 incl refund + caps; actions 8 incl refund + admin role-gate auth-on; auth 11); conservation invariant updated to (inward - refunds == invoice.paid + credit). ⭐ LIVE STRESS TEST (script/stress/settlement_live.rb) on REAL account L0001 (#662, 5 outstanding invoices, baseline imbalance=0 → imported data is internally consistent): 8/8 scenarios PASS (overpay→credit, apply-credit, reverse, deposit, account multi-invoice payment, write-off, refund, statement renders %PDF), conservation imbalance stayed 0 throughout, and RESTORE VERIFIED byte-identical (snapshot→run→restore; Paul authorised live testing as DB will be refreshed pre-go-live). SETTLEMENT now ~9.3/10.
⭐ LIVE SELF-IMPROVEMENT LOOP (Paul, 2026-06-07 13:37→16:00, ≤3min cadence): stress-test on REAL data, find+fix issues, Perplexity-researched. CYCLE 1 (~13:45): live data-integrity audit (script/stress/live_audit.rb, read-only) across whole DB → money/status integrity ALL CLEAN (0 overpay-leaks, 0 status mismatches, 0 negative money, 0 line/header drift, 0 double-bookings (provider EXCLUDE constraint works), 0 orphan treatment items). FOUND: 1 fully-orphan payment #8686 (R50 cash, no invoice/account/patient, from 06-06). FIX (Perplexity-grounded: every payment must identify whose money it is): Payment#must_have_an_owner validation (rejects payment with no invoice/account/patient) + 2 specs (settlement_spec 16/16 green). #8686 left for the pre-go-live DB refresh; new orphans now blocked. CYCLE 2 (~13:47, appointments/diary): CLEAN — overlap-prevention + end<start guards both reject on real data (rollback-verified; the overlap guard even blocked a test slot that collided with a real booking). No fix needed. ⭐ CYCLE 3 (~13:52, estimates): FOUND+FIXED a real concurrency bug — Estimate#accept_and_invoice! had no model-level guard, so a double-submit/race created TWO invoices (confirmed on real data: invoices_created=2). FIX (Perplexity-grounded — pessimistic `lock!` + `raise RecordInvalid if status==accepted` inside the txn; unique-constraint = belt-and-braces follow-up): re-verified on real data invoices_created=1, 2nd call blocked. Controller already rescues RecordInvalid. ⭐⭐ CYCLE 4 (~14:00, COT→invoice): FOUND+FIXED a real DOUBLE-BILLING bug — generate_invoice twice on a COT created two invoices billing the SAME completed items (confirmed on real COT #79: 2 invoices, same R85 line). FIX (Perplexity-grounded — bill only completed items NOT already on a non-void invoice line; void releases them; supports staged billing): Invoice.from_course now excludes already-billed treatment_items + courses_of_treatment_controller#generate_invoice rejects an empty invoice. Verified on real data: fresh completed item bills on 1st generate, NOT on 2nd (true/false); an already-invoiced item (COT#79) correctly yields 0 lines. Note: also exposed that double-generate was a genuine risk, not just 'latest wins'. ⭐ CYCLE 5 (~14:05, money param hardening): FOUND+FIXED integer-overflow — a huge amount (e.g. R99,999,999 typo → 9.99B cents > int max 2.147B) raised ActiveModel::RangeError → 500 (uncaught). FIX (Perplexity-grounded — cap money below column range): Payment::MAX_CENTS=2_000_000_000 + numericality less_than_or_equal_to; payments_controller + billing_accounts_controller (receive_payment/deposit/refund) reject amounts >R20m with friendly alert. Verified: model huge_rejected=true; live huge deposit to real acct 1618 → 303 friendly redirect (no 500, no mutation). Param parse note: 'abc'.to_f→0 (guarded), ' 50 '→50 ok. ⚠️ PAUL FINDING (~14:08) — imported estimates show a TOTAL but NO CODES/line items: ROOT CAUSE = the Elixir export tmp/estimates.json was HEADER-ONLY (each record = account_code + patient_name + value(total) + free-text `details` like "CHECK UP, CLEAN AND EMAX CROWN" + date — NO structured line items), so import_elixir_estimates.rb created estimates with total_cents but ZERO estimate_lines (4225 of 4236 estimates affected). NOT a display bug; the codes were never in the source. FIX FORWARD: updated import_elixir_estimates.rb to build real estimate_lines from a `lines:[{code,description,fee/amount,quantity,tooth}]` array when present (matches ProcedureCode by code, recalculates), header-only fallback retained — READY for the planned DB refresh. REQUIRES: a proper Elixir re-export that INCLUDES estimate line detail (codes live in Elixir's estimate-detail/tariff tables, readable; the old export omitted them). Did NOT fabricate codes from the description+total (would be inventing billing data). ⭐ CYCLE 6 (~14:12, performance): FIXED N+1 on BillingAccountsController#show — the members list ran ~2 queries/member (6 members = 14 queries, scaled linearly) for next-appointment + outstanding; now TWO batched queries (Appointment grouped + Invoice grouped). Verified: 14→2 queries, page 200. ⚠️ PAUL FINDING #2 (~14:12) — invoice showing 'Private' medical aid (Morne Ayres #7, INV-2026-004843 Botox): NOT a bug — that patient genuinely has 0 scheme_memberships (and Botox=aesthetic=private/cash anyway), so '— Private —' is CORRECT. The '7112085259089' on his statement is his SA ID NUMBER (born 1971-12-08), not a scheme member number. Medical aid IS imported + shows for 1317 of 2505 patients (53%); 47% recorded private. So NOT a systemic gap like the estimate codes. If a SPECIFIC patient who should have medical aid shows private, that's a per-patient import-completeness check vs Elixir (name the patient → trace). ⭐⭐⭐ NEW FEATURE (Paul, ~14:25) — AI ESTIMATE COMPOSER built+live: AiService#compose_treatment_lines (Claude sonnet; system prompt = the practice's 190-code catalogue + active TreatmentMacros; returns JSON {lines:[{code,tooth_number,quantity}],notes}); estimates#ai_compose (POST /estimates/:id/ai_compose, member route) creates estimate_lines (matches ProcedureCode by code, recalc, audit); EstimateShow.jsx AI box ('Describe the treatment — AI fills in the codes', shown when !isAccepted). TESTED real prompts: 'two crowns on 21 and 11' → crown+block+CAD/CAM+bonding+periapical PER TOOTH, shared items (infection control/steri/anaesthetic/rubber dam) billed ONCE; 'root canal on 34' → RCT bundle + noted premolar/2nd-canal; e2e on demo 2985 added 9 lines R11917, cleaned up. ⚠️ INCIDENT: broken JSX took the rig DOWN briefly — the boot command runs `bin/vite build` so a bad .jsx crash-loops the container; FIX RULE: always `bin/vite build` and confirm success BEFORE `restart web`; recovered (2733 modules ✓). PAUL DIRECTION (~14:30): (a) LEARN from real historical estimates (sibling folders ../../'Dr Chalita Transactions Check' + ../../'Dr Chalita Filing' + their existing estimate-AI tool) to teach the composer the practice's exact treatment→code→fee patterns + note style; (b) DENTAL CHART per patient profile (red=needs-work/planned, black=done/existing) — infra EXISTS (ToothChartEntry conditions + Odontogram.jsx + ToothActionModal) but NOT a PatientShow tab; build the tab. LOOP now re-pointed to iterate THIS flow (AI estimate + chart) best-in-market via Perplexity research. ⭐⭐⭐ COMPOSER UPGRADED ~14:40 (Paul: 'use the estimate agent we've built'): found the practice's existing GPT 'Estimate Assistant' in ../../Dr Chalita Filing/3. Estimates/_GPT Estimate Assistant/ (Estimate SOP, Charge Codes Glossary 226L, ICD-10 31L, Treatment Code Recipes & Benchmarks 174L — setup codes, RCT canal-by-tooth table, materials/lab/sundries, 'GIVE MORE CODES NOT FEWER', ICD-10 per treatment, pricing-from-Elixir rule). Copied the 3 de-identified knowledge files into repo system/reference/estimate_assistant/ + rewired AiService#compose_treatment_lines to use them as the authoritative system prompt (recipes + ICD-10) grounded on the live catalogue (valid codes + current fees); estimate_lines now get icd10_code. VERIFIED real: 'filling on 36' → setup(8109×2,8110,8145,8304)+8107+8369+K02.8 (noted assumed 3-surface); 'root canal on 26' → setup+8107×4+8124+8339+8340×2 (canal table!)+8639+8306+8398+8369, ICD-10 K04.0, noted upper-molar 3-canal. Now codes like an experienced biller using THE PRACTICE'S OWN recipes. up=200. NEXT: handle handwritten-chart colour split (file 8) + 'from photo to phased estimate' (file 9) for the chart feature; surface AI note/confidence in the UI; build the Dental Chart tab. Original alert kept below for history ⬇️
>
> ⚠️⚠️ INFRA-ALERT FOR PAUL (2026-06-07 ~00:42): the rig has been UNREACHABLE — both HTTP (:3000, 000 @30s) AND SSH (:22, connection timed out) — almost continuously since ~23:00 (down at 23:48/23:56/00:01/00:06/00:17/00:22/00:27/00:32/00:37/00:42; only a brief HTTP-only blip at 00:11). That's ~1h40m+ and counting. This coincides with the nightly encrypted backup, which is evidently heavy enough to take the whole box offline. IMPLICATIONS: (1) the LIVE WhatsApp bridge is almost certainly unreachable for this entire window — patient messages around midnight may not be received/answered; (2) the deep-work stress tests are blocked on this (scripts staged, will run the moment SSH returns). RECOMMENDATIONS: move the nightly backup well off-peak; throttle it (nice/ionice or backup-tool bandwidth/IO limits); and VERIFY it isn't hanging/looping (a healthy backup of this DB should not take >1.5h of full saturation). The loop is NOT attempting any restart (not its call) — just monitoring + retrying every 5 min.

**DIRECTIVE:** rig now on Ethernet (Wi-Fi flapping fixed). Focus the self-improvement loop on the PATIENT JOURNEY end-to-end — make it BETTER + EASIER. User-friendliness is the #1 priority (over new capability), especially BUTTONS + KEYBOARD to make things quicker, and being PREDICTIVE to save the user time. Also ensure feature parity with leading dental software. Hard constraints unchanged (no Elixir writes, no login resets, no real-data mutation). Run until 2026-06-07 07:00; keep questions aside for Paul.
**Paul's explicit asks (work queue):**
1. ✅ BACK-NAVIGATION glitch (cycle 1, DONE) — Back from an estimate opened inside a patient account dropped you on "All estimates" instead of the patient. FIX: new `app/javascript/components/SmartBack.jsx` (browser history.back() with fallback to index) wired into EstimateShow/InvoiceShow/CourseOfTreatmentShow (replaced hardcoded "All estimates/invoices/courses" links). Verified all 3 show pages 200 + SmartBack in bundle. Pattern: reuse SmartBack on other show pages too.
2. ESTIMATE CODE ENTRY UX — ✅ CORE DONE (cycle 3): EstimateShow.jsx line editor is now keyboard-first — Enter picks the top autocomplete match, Enter again adds the line + refocuses the code box (rapid entry), Esc clears; tooth/qty/fee accept Enter; added codeInputRef + onCodeKey/onFieldKey + a kbd hint. Autocomplete (code OR description) already existed. STILL TODO: favourites/recent quick-add bar + macro/bundle picker on the estimate editor; apply the same keyboard flow to InvoiceShow + COT add-procedure.
   RESEARCH (cycle 2, Perplexity) saved at system/memory/ux_research_2026-06-06.md — drives the flow-first order: smart code entry → command palette (Ctrl+K) → keyboard shortcuts → predictive pre-load → global omnibox → statement/estimate layout → context-aware diary actions → inline validation.
3. PREDICTIVE PRE-LOAD from the diary — when a patient comes for a consultation/check-up, pre-load the invoice/estimate with the codes normally expected for that visit type (read the appointment reason/type), so reception just checks + removes rather than building from scratch. Use AI to predict; mark clearly "AI suggestion — review".
4. LAYOUT of STATEMENTS + ESTIMATES — improve against good practice (research via Perplexity per Paul's global pref).
5. KEYBOARD shortcuts / Enter throughout the journey + buttons for speed.
6. PREDICTIVE AI in the system (not just for dev) — suggest text (e.g. review messages, notes) flagged clearly as needs-review.
7. Feature-parity check vs leading dental software (benchmark a NEW aspect each cycle, no repeats).
PRIORITISE quick high-value wins first (back-nav ✓, then Enter-to-new-line, keyboard), then the bigger predictive-preload feature.

⭐ ALREADY-EXISTS (cycle 4 discovery — DON'T rebuild, enhance only): `app/javascript/components/GlobalSearch.jsx` = a command-palette-style global search (was `/`, NOW ALSO Ctrl/Cmd+K — cycle 4 added the chord + placeholder hint; searches patients/invoices/estimates/procedure_codes via /search, ArrowKeys+Enter nav) AND `GlobalShortcuts.jsx` = keyboard shortcuts (g=go-to menu, n=new patient, b=new booking, Esc, '?' help overlay). Both mounted in DashboardLayout. So queue items "command palette" + "keyboard shortcuts" are LARGELY DONE — future cycles should EXTEND these (more actions, discoverability, type-ahead on dropdowns) not duplicate. Cycle 4 also: deleted a duplicate CommandPalette.jsx I had drafted before finding GlobalSearch (good: check-what-exists rule).
CYCLE 5 ✅ — estimate FAVOURITES quick-add chips: estimates_controller#render_inertia now passes `favourite_codes` (top 8 most-used ProcedureCodes via EstimateLine group/count); EstimateShow.jsx renders them as 1-click "Quick add" chips (quickAdd → POST line, no typing). Verified estimate/31 200, query returns 3 codes today. Reversible (one controller block + one JSX block).
CYCLE 6 ✅ — VISIT-BUNDLE MACROS on the estimate editor (building block for predictive): new `estimates#apply_macro` (config/routes.rb member POST :apply_macro; estimates_controller.rb mirrors COT apply_macro — creates estimate_lines from TreatmentMacro items, recalc, audit); render_inertia passes `treatment_macros` (20 active); EstimateShow.jsx has `applyMacro` + a "+ Visit bundle…" select in the Quick-add row. Verified estimate/31 200, apply_macro_estimate route present, 20 macros. Reversible (1 route line + 1 controller action + 1 prop + 1 JSX select + applyMacro fn).
CYCLE 7 ✅ — PREDICTIVE layer (Paul's marquee 'save time' ask) DONE: estimates_controller#render_inertia now finds the patient's next today-or-upcoming Appointment, reads its free-text `reason`, and via new private `suggested_macros_for(reason)` (keyword→macro map) passes `visit_reason` + `suggested_macros`. EstimateShow.jsx renders an amber 'Suggested for this visit — "<reason>" · please review' block with one-click bundle buttons (reuse applyMacro) + 'AI-suggested from the booking — check before sending'. Matcher verified: 'Check up and clean'→C/U,C/U & CL,PROPHY; 'Crown prep'→CROWN; 'whitening consult'→C/U,WHITENING; 'random visit'→none (conservative). estimate/31 200. Reversible (1 helper + locals + 2 props + 1 JSX block). FUTURE: Anthropic refinement of reason→codes; apply same predictive block to the COT page.
CYCLE 8 ✅ — COT AddProcedureModal.jsx keyboard flow (consistency with estimate editor): added onSearchKey (↑/↓ move highlight, Enter picks top then Enter submits) + onToothKey (Enter submits) + a kbd hint; wired onKeyDown to the search + tooth inputs. Verified courses-of-treatment/79 200. Reversible (2 handlers + 2 onKeyDown + 1 hint).
CYCLE 9 ✅ — CONTEXT-AWARE DIARY ACTION: AppointmentDetailModal.jsx now has a prominent primary "Start estimate for this visit" button (router.post /patients/:id/estimates → draft + redirect to editor, which then pre-suggests codes from the appointment reason — ties cycles 5-7 together). Modal already had Open-profile + View-treatment-plans links. Verified diary 200. Reversible (1 button block). NOTE: clicking creates a draft estimate (intentional; drafts deletable).
CYCLE 10 ✅ — PREDICTIVE block on the COT (treatment-plan) editor too: new shared concern app/controllers/concerns/visit_suggestions.rb (next_visit_for + suggested_macros_for) included in CoursesOfTreatmentController; #show now passes visit_reason + suggested_macros; CourseOfTreatmentShow.jsx renders the amber 'Suggested for this visit — review' block (one-click applyMacro, reuses existing COT apply_macro). Verified cot/79 200, both controllers syntax OK. NOTE: estimates_controller still has its own private suggested_macros_for copy (works) — future dedupe to use the concern. Reversible (1 concern + 3-line show change + 1 JSX block + 2 props).
CYCLE 11 ✅ — PAYMENT MODAL keyboard + inline validation (PaymentModal.jsx): amount auto-focuses+selects on open; Enter submits; invalid amount → red inline error that KEEPS focus + typed value (no toast-and-lose-context, research 'harsh errors' hate); overpayment shows 'R X over the balance — recorded as a patient credit' (also resolves design-note #1). Verified invoices/2844 200. Reversible (amountRef + amountError + overpay + onKeyDown + inline messages).
CYCLE 12 ✅ — FEATURE-PARITY BENCHMARK (front-desk) via Perplexity → system/memory/feature_parity_frontdesk_2026-06-06.md. Verdict: Ivory has all the CATEGORIES (WhatsApp-AI booking, reminders, ASAP waitlist, recalls, intake, estimate/invoice/payment, reporting); gaps are the 'automated + measured' depth layer. Also confirmed (CHECK-first paid off): InvoiceShow 'Record payment' ALREADY a prominent emerald primary button w/ balance; AppointmentFormModal ALREADY has zod inline validation + predictive duration-from-reason; booking is mature. 🟢 small candidate builds queued: WhatsApp quick-reply templates (#5), confirmation-status chip + 'unconfirmed today' (#1), recall pipeline counts (#4). 🔴 Paul-decision: text-to-pay/card-on-file (#7), full reminder/recall sequences+analytics (#1/#4/#8).
CYCLE 13 ✅ — WhatsApp QUICK-REPLY templates extended: ConversationShow.jsx CANNED_REPLIES already had 6 (Greeting/Directions/Appt confirmed/Rooms hours/Public holiday/Banking); ADDED 2 compliance-safe journey ones — 'Reschedule' + 'Running late' (no after-hours/weekend promises). Verified conversations/82 200. Reversible (2 array entries). NOTE: another already-built surface (templates existed) — extended not rebuilt.
CYCLE 14 ✅ — RECALL PIPELINE COUNTS (parity gap #4): recalls_controller#index stats now also computes contacted/booked/overdue; Recalls.jsx renders a 4-chip stat row (Due+overdue / Contacted / Booked / Total) above the list. Verified recalls 200, syntax OK. Reversible (stats hash + 1 JSX block).
CYCLE 15 ✅ — DEDUPE: estimates_controller now `include VisitSuggestions` + uses next_visit_for; removed its private suggested_macros_for copy (keyword map now single-source in the concern, shared with COT). Verified syntax OK, estimate/31 200, matcher 'check up and clean'→C/U,C/U & CL,PROPHY unchanged. (Concern methods are public on the controllers — fine.) Reversible. Paul can tweak the visit→bundle mapping in ONE file now: app/controllers/concerns/visit_suggestions.rb.
CYCLE 16 ✅ — CHARTING/CASE-ACCEPTANCE parity benchmark (Perplexity) appended to feature_parity_frontdesk_2026-06-06.md Part 2. Confirmed mature: charting (odontogram+macros+predictive), patient-view, medical/self split. Also confirmed already-built: PatientShow quick-actions (Book/Edit/Request-review/new-COT), Reminders stats (pending/today/tomorrow/flagged + chips). GENUINE GAPS found: #6 good/better/best alternative plans (medium, Paul steer); #7 ⭐ ACCEPTED-BUT-UNSCHEDULED TREATMENT worklist (HIGH VALUE, read-only buildable — TreatmentItem status=planned for patients w/ no upcoming appointment = lost production).
CYCLE 17 ✅ — UNSCHEDULED-TREATMENT WORKLIST (parity gap #7, revenue recovery): pages_controller#dashboard now builds `unscheduled_treatment` via new read-only helper build_unscheduled_treatment (TreatmentItem status=planned grouped by patient, MINUS patients with an upcoming non-cancelled appointment; returns patient + item_count + Rand value, top 12 by value, 2-3 queries no N+1). Dashboard.jsx renders an amber 'Unscheduled treatment — rebook' card (click-through to patient). Verified dashboard 200, query 0 rows today (activates with data). BUG FOUND+FIXED mid-cycle: Rails 8 `pluck` with raw SQL aggregates (COUNT(*), COALESCE(SUM...)) raises UnknownAttributeReference → must wrap each in Arel.sql(). Reversible (1 page_data entry + 1 helper + 1 prop + 1 JSX card).
CYCLE 18 — VERIFY-ONLY (clean): regression sweep of all surfaces touched this session — / · /estimates/31 · /courses-of-treatment/79 · /invoices/2844 · /recalls · /reminders · /conversations/82 · /settings · /diary · /patients ALL 200. No regressions across the 17 build cycles. (Disciplined verify cycle — no cosmetic changes invented; most remaining items are Paul-decisions.)
CYCLE 19 — VERIFY-ONLY: no StatementShow.jsx (statements are server-rendered PDFs at /accounts/:id/statement.pdf?from=&to=). Verified statement PDF generates: HTTP 200, valid %PDF, 9237 bytes (statement_pdf.rb + winansi fix solid). Nothing to polish (PDF path, risky to touch). No code change.
PERF WATCH (cycle 23-24, for Paul — RESOLVED/benign): /estimates INDEX cold-loaded slow once (>12s→000, then 200 within 20s on retry). Cycle 24 CHECKED the query — it is ALREADY optimised: `Estimate.includes(:patient, :estimate_lines).order(created_at: :desc).limit(300)` (no N+1, bounded). So the blip was Rails/Vite cold-start warm-up after idle, NOT a code/query issue. No action needed.
NIGHT NOTE (cycles ~28-30, ~22:57-23:09, for Paul — likely benign): the /up health probe slowed to >20s twice around 23:00 (200 on longer-timeout retry). App still serves. Most likely the NIGHTLY ENCRYPTED BACKUP loading the rig at this hour (expected). If app responsiveness is poor for users around backup time, consider scheduling the backup off-peak. Not a regression.

⭐ DEEP-WORK PIVOT (Paul chose 'substantive deep work' over idle probes, ~23:44). Plan: STRESS-TEST + HARDEN tonight's 14 new features (build→audit→harden). Progress: (a) adversarial estimates#apply_macro with INVALID/missing macro_id → HTTP 404 graceful (NOT 500) ✓. (b) Wrote 2 SAFE stress scripts (ready, deploy+run when rig free): script/stress/apply_macro_e2e.rb (creates throwaway estimate on demo patient 2985 'John Demo-Smith', applies a macro, asserts line-count/fees/total, then DESTROYS it) + script/stress/unscheduled_treatment_edge.rb (transaction-ROLLED-BACK edge test: planned-treatment patient appears in build_unscheduled_treatment only with no upcoming non-cancelled appt). (c) ~23:48-01:36 the rig was UNRESPONSIVE/oscillating (HTTP 000 + SSH timeout) — nightly backup; stress runs deferred. (d) SSH recovered 01:36 → STRESS RESULTS: ✅ T1 apply_macro_e2e PASS (C/U macro → 5 lines, ok_fees=true, total R1922.79 == line-sum, self-cleaned/destroyed test estimate #4291); ⚠️→✅ T2 unscheduled_treatment_edge initially FAIL (false/false/false) — DIAGNOSED as a TEST FLAW not a code bug: demo patient 2985 already has 3 upcoming appts so the query CORRECTLY excluded them; FIXED the test (cancel pre-existing upcoming appts inside the rollback first) → re-ran PASS (no-appt=true, upcoming=false, cancelled=true; 0 persisted). CONCLUSION: estimates#apply_macro + pages_controller#build_unscheduled_treatment are VERIFIED CORRECT end-to-end. Also verified post-outage: tonight's feature pages (/ , /estimates/31, /courses-of-treatment/79, /recalls) render 200, apply_macro bad-id → 404 — no corruption from the outage. (e) STRESS 01:41 — PREDICTIVE MATCHER BREADTH PASS: ran suggested_macros_for over 20 varied reasons → ERRORS=0; known visit types map correctly (root canal→3 RCT bundles, implant consult→C/U+IP, recement crown→CROWN+RECEMENT, scale and polish→C/U & CL+PROPHY); unknown/empty/irrelevant ('extraction','emergency pain','xyz gibberish','')→suggests NOTHING (conservative, no false suggestions). MINOR CONTENT NOTE for Paul: 'extraction' maps to nothing because there's NO extraction TreatmentMacro — a macro-library gap (not a code bug); consider adding an extraction bundle.
(f) STRESS 01:46 — INVOICE PAYMENT GUARDS PASS (script/stress/invoice_payment_guards.rb, rolled-back txn): lifecycle partial→part_paid, complete→paid ✓; non-positive (-500,0) IGNORED ✓; VOIDED invoice payment BLOCKED (stays void, paid unchanged) ✓; rollback clean (demo-2985 invoices=3 unchanged). STRESS SCORECARD (5 PASS): apply_macro bad-id→404 · apply_macro_e2e · unscheduled_treatment_edge · predictive matcher breadth · invoice payment guards. Tonight's new code + key hardening all VERIFIED.
(g) STRESS 01:52 — EMPTY-ESTIMATE ACCEPT GUARD PASS (end-to-end via real controller + self-clean): created empty draft estimate #4292 → POST accept_and_invoice → HTTP 303 (redirect+alert), INV_AFTER=3 (NO junk invoice created), STATUS=draft (not accepted); destroyed #4292, ESTIMATES_NOW=3. STRESS SCORECARD (6 PASS): apply_macro bad-id→404 · apply_macro_e2e · unscheduled_treatment_edge · predictive matcher breadth · invoice payment guards · empty-estimate accept guard. All of tonight's significant new logic + guards VERIFIED; demo-2985 counts unchanged (estimates=3, invoices=3).
(h) STRESS 01:56 — FAVOURITE_CODES QUERY PASS (read-only): top-used codes (fav_ids=3) all resolve to ACTIVE ProcedureCodes (all_active=true), fee mapping crash-safe (no_fee_crash=true; sample 8110/R146.36, 8109/R66.94, 0000/R85.00); filter_map correctly drops inactive/missing.
⭐ STRESS COVERAGE COMPLETE (7 PASS, all green) — EVERY significant NEW code path from tonight is verified: apply_macro (bad-id 404 + e2e), unscheduled_treatment query, predictive matcher breadth, invoice payment guards (lifecycle+void+non-positive), empty-estimate accept guard, favourite_codes query. No bugs found in tonight's code; one CONTENT gap noted (no extraction macro). demo-2985 untouched (est=3, inv=3, 3 appts). Deep-work hardening phase DONE.
NEXT: one more data-point for Paul — double-generate-invoice 'latest wins' confirmation (design note, NOT a fix) — then lighter verify cycles to 07:00 (don't invent cosmetic changes; app is mature + verified). Stress scripts in script/stress/.
NEXT BEST (mostly Paul-decision items remain): good/better/best alternative plans (#6); text-to-pay (#7); automated reminder/recall sequences (#1/#4/#8); tooth-chart on patient estimate PDF (#4 minor/risky); another genuinely-missing small win, a fresh parity micro-benchmark on a NEW area, OR lighter verify cycles. App is MATURE + journey overhaul COMPLETE — be disciplined, keep loop alive to 7am, don't invent cosmetic changes.

## Stress-test phase (2026-06-06, session 6 — cycles 97+)

> ⚠️ INFRA NOTE for Paul: the rig's Tailscale link (100.73.38.21) has been FLAPPING since ~14:18 on 2026-06-06 (down 14:18-14:29, up 14:39/14:45, down 14:51/14:58). Stress cycles keep probing + resume when reachable; no data affected (failed SSH attempts never connect). If this persists it's worth checking the rig's Tailscale/network — not an app issue.

- **Cycle 110-114 — batch 12 (secondary surfaces) progress despite the rig SSH flapping; all graceful so far.** WORKAROUND DISCOVERED: when the rig's SSH (port 22) is flapping but HTTP (port 3000) is up, run the no-ID/no-cleanup POST tests from the dev PC as `curl -H 'Host: 10.0.0.125' -X POST http://100.73.38.21:3000/...` — the LAN Host header grants the CSRF+auth exemption over the Tailscale HTTP connection (verified). Batch-12 results (COMPLETE, ALL CLEAN — no 500s, no leftovers): reporting?from=notadate&to=alsobad → 200; analytics?range=garbage → 200; calendar_notes#create empty body → 303 friendly + verified NO blank note persisted; imaging#scan garbage payload → 302 graceful; courses_of_treatment#apply_macro unknown id → 404 graceful (RecordNotFound); conversations#reply empty/whitespace body → guarded (code-confirmed conversations_controller.rb:69 `body.blank? → "Reply cannot be empty."` BEFORE any Twilio send — verified by reading, not live-tested, to avoid a real send). Leftover-cleanup confirmed: 0 blank calendar_notes, 0 empty demo COTs.

- **Cycle 120 — light verify cycle (all clean):** /webhooks/voice with empty + garbage params → HTTP 403 (Twilio signature validation rejects unsigned/forged, like the WhatsApp webhook — no 500, no outbound; webhook security consistent across both channels). Confirmed script/data-cleanup/ambiguous_phones.rb + zero_total_estimate.rb deployed + ruby -c Syntax OK. Major work complete; maintaining to the 7-Jun-13:00 report.
- **Cycle 119 — DATA ITEM #4 (Morne Maartens) READ-ONLY inspection done — merge is SIMPLER than expected + #2439 is a DIFFERENT issue.** Findings for Paul (NO change made): (a) Morne Maartens duplicate = patient #1 vs #1191. #1191 is CANONICAL/active (acct M0186, phone +27833724919, 3 invoices + 1 estimate). #1 is the EMPTY duplicate (acct "00", no phone, 0 appts/0 inv/0 est/0 cot, just 1 self AccountPatient link) → so this is NOT a complex merge: #1 references nothing, so once its one AccountPatient link is removed, #1 can simply be DELETED. ⚠️ The two ID numbers differ in the last 3 digits — #1=9101265106008 vs #1191=9101265106085 — Paul should confirm the correct SA ID for the canonical #1191 before deleting #1. (b) #1193 Ilze-Mari Maartens (idno 7910040014089) is a DIFFERENT person, same surname — leave alone. (c) PLACEHOLDER #2439 is NOT a Maartens record — it's "LEON CLARK - KONTAK HOM VIR NUWE AFSPRAAK" (idno=ELX-IMPORT-1, no phone, 2 appointments) — an Elixir import where the NAME field holds an instruction ("contact him for a new appointment"). Separate data-quality item: fix the name to just "Leon Clark" + review the 2 appointments. ACTION FOR PAUL: confirm canonical Morne ID → then a tiny script can unlink+delete the empty #1; separately decide what to do with the Leon Clark #2439 placeholder. No merge script written yet (needs Paul's canonical-ID confirmation).
- **Cycle 118 — DATA ITEM #5 (zero-total estimate) script DONE + DATA ITEM #2 (VAT backfill) DOCUMENTED as likely-moot.** (5) Wrote + dry-run-verified script/data-cleanup/zero_total_estimate.rb: DRY-RUN (default) lists zero-total estimates + flags delete-candidates (empty + >1yr + draft + non-demo); APPLY=1 (env-gated, NOT run) deletes them in a transaction. Only 1 real candidate: #31 EST-2026-000001 (Lance Van Vuuuren, draft, 0 lines, 2022-10-11). NOTE: cleaned my own test-leftover zero-estimate #4281 (demo patient, today) first. (2) VAT backfill CONCLUSION for Paul: InvoiceLine + EstimateLine DO have a vat_cents column, but 20,729 of 20,753 invoice lines have it 0/null (the historical Elixir-imported lines). The practice is VAT-registered (vat 4260308871) + prices 15% VAT-INCLUSIVE, so per-line VAT is DERIVABLE (line_vat = round(line_total × 15 / 115)) and the invoice-level vat_cents total is what appears on PDFs/statements. So a 20k-line backfill is LIKELY UNNECESSARY + RISKY (large mutation of a probably-derived/unused field). RECOMMENDATION: do NOT backfill unless an accountant confirms per-line VAT is required on historical invoices; if ever needed the formula is line_vat = round(line_total_cents × 15 / 115). No script written (correctly — don't invent a risky mutation). DATA STATUS: #3 phones=report done, #5 zero-estimate=script done, #2 VAT=documented-moot; REMAINING need Paul's decision: #1 (52 credits R87,638 — financial treatment), #4 (Morne Maartens merge — canonical record).
- **Cycle 117 — DATA ITEM #3 (ambiguous phones) DELIVERED as a read-only report (no mutation).** Wrote + verified script/data-cleanup/ambiguous_phones.rb (REPORT-ONLY, no APPLY — auto-"fixing" an ambiguous number could write a WRONG number). Of 1868 patients with a phone, 10 don't normalise to +27XXXXXXXXX: (a) 3 LEGIT FOREIGN — leave alone: #27 Ronel Arnold +64 (NZ), #937 Salome Lochner +31 (NL), #1753 Joanne Taylor +64 (NZ); (b) 7 SA WITH AN EXTRA DIGIT (12 digits, should be 11) needing MANUAL reception correction against the patient's real number: #572 Hlatshwayo +277382969165, #674 Jonker +278282447788, #1039 Maritz +277358362828, #1461 Peyper +276219002152, #1686 Steyn +277155859101, #1745 Mphepo +277345545433, #2613 Gouws +277245230327. ACTION FOR PAUL: reception edits those 7 by hand after confirming the real number. Re-run anytime: `bin/rails runner script/data-cleanup/ambiguous_phones.rb`.
- **Cycle 116 — both rig ports down; wrote the DATA-CLEANUP README (rig-independent).** Created le-roux-repo/script/data-cleanup/README.md — documents the 5 data items Paul flagged (52 credits, per-line VAT backfill, ~5 ambiguous phones, Morne-Maartens-merge+#2439, zero-total estimate) with a status table + the safe-handling protocol every script will follow (DRY-RUN default, APPLY=1 env-gated + transaction-wrapped, backup-first, rollback note, never auto-run). Items 1 (credits — financial treatment) and 4 (merge — canonical record) need Paul's decision before scripting; 2/3/5 are scriptable after read-only inspection. Actual <item>.rb scripts to be added one at a time (with verified dry-runs) when the rig link is stable.
- **Cycle 115 — batch 13 (untested/secondary surfaces) ALL CLEAN.** GET /api/v1/scribe/heartbeat without token → 401, with a bad token → 401 (token-protected, not 500); POST /notifications/mark_all_read → 200 graceful; GET /mail → 200 (renders, empty-state ok); GET /reconciliation → 200; POST /webhooks/whatsapp with empty-body+fake-From AND with garbage params → 403 (the Twilio X-Twilio-Signature validation rejects unsigned/forged requests — correct security, no outbound message, no 500). So the scribe API (token auth), notifications, mail, reconciliation, and the WhatsApp webhook (signature auth) are all robust against unauthenticated/malformed input. Core + secondary surfaces now broadly stress-covered.

### ⭐ STRESS COVERAGE STATUS for Paul (as of cycle 110, ~14:51)
**(a) Hardened / bugs fixed this stress phase (all reversible via git):**
- 🐞 unicode/emoji/macron/Greek/Cyrillic in a patient name crashed invoice/estimate/statement/receipt PDFs → `winansi()` sanitizer in document_pdf.rb + statement_pdf.rb + receipt_pdf.rb (IntakePdf already safe).
- 🐞 paying a VOIDED invoice was allowed + silently un-voided it → guards in payments_controller#create + Invoice#register_payment!.
- 🐞 accepting a ZERO-LINE estimate created an empty R0 invoice → estimates#accept_and_invoice empty-lines guard.
- 🛡️ register_payment! ignores non-positive amounts (defense-in-depth).
- 🛡️ procedure_codes#bulk_update percent bounded to -90%..100% (was unbounded → fat-finger could ×N or negate the whole live fee schedule).
- ✅ Verified-solid (no change needed): double-booking blocked (even under 2-thread concurrency), end<start blocked, public-host→401, sparse/120-line/single-day PDFs, malformed controller params (no 500s), audit-log no-false-log, search special-chars/empty/2000-char, payments sum, blank-phone+past reminders graceful, intake-empty→422, re-cancel idempotent, patient-delete-with-open-invoice blocked by FK, set_status guarded, negative line-qty normalized.

**(b) 3 DESIGN-NOTES needing Paul's decision (not bugs — judgment calls):**
1. Overpay an invoice → negative balance = a patient CREDIT (no cap; correct accounting). Decide: surface it as "credit" in the UI?
2. Double-click "Generate invoice" → 2 invoices ("latest wins", by design for re-invoicing). Decide: make idempotent (redirect if COT already has a non-void invoice)?
3. Appointment overlap guard is APP-LEVEL (held under a 2-thread test but Ruby GVL limits the test). Decide: add a Postgres EXCLUDE constraint (btree_gist) for true-concurrency safety? (Low risk on the current single-worker setup.)

**(c) UNTESTED surfaces (next stress targets after the core-flow saturation):** mail/inbox controllers, scribe-daemon API (api/v1/scribe), imaging#scan, reconciliation, voice webhooks (webhooks/voice*), analytics#index, notifications. Core booking/billing/PDF/intake flows are now well-covered.


- **Cycle 109 — HARDENING: bulk fee-uplift had no percent bounds (fat-finger risk on the LIVE fee schedule).** procedure_codes#bulk_update applied `1 + pct/100` to EVERY default_fee_cents immediately (no preview/transaction), so a typo like 600 (meant 6) would 7× the whole schedule and pct < -100 would make fees NEGATIVE. FIX: bound `pct < -90 || pct > 100 → reject with alert "Uplift must be between -90% and 100%"`. VERIFIED on the rig: uplift 600% + -200% both → 303 rejected, fee-sum unchanged (31,222,650 before==after); normal in-range path untouched. Other batch-11 checks clean: appointments#set_status to an invalid value is already guarded (STATUS_TRANSITIONS allowlist → "Unknown status"); a negative estimate-line quantity is normalized (qty→1, line_total recomputed positive — no negative corruption). NOT yet tested: conversations#reply empty body, calendar_notes#create empty, imaging#scan bad payload — next cycle. RUNNING TALLY (stress phase): 3 real bugs FIXED (unicode-PDF crash, pay-voided-invoice, empty-estimate→invoice) + 2 hardenings (register_payment! non-positive, bulk-uplift bounds) + 3 design-notes for Paul (overpay→credit, double-generate_invoice→2-invoices, appointment EXCLUDE-constraint).
- **Cycle 108 — REAL FIX: accepting a ZERO-LINE estimate created an empty R0 invoice.** estimates#accept_and_invoice had no empty-estimate guard, so converting a draft with no lines made a meaningless R0 invoice + burnt a sequence number. FIX: added `if estimate.estimate_lines.empty? → redirect with alert "Add at least one line before converting this estimate to an invoice"` (mirrors generate_invoice's "mark an item Done first" guard). VERIFIED: zero-line accept → 303, 0 invoices; with-line accept → 303, 1 invoice (no regression). Rest of batch 10 CLEAN: single-day statement (from==to) renders valid PDF; deleting a patient with an OPEN invoice is blocked by the FK constraint (no orphans); recall PATCH bad status → 303 friendly; notification mark_read on a nonexistent id → 404 (not 500); /diary renders 200 in 29ms. Cleaned all test data.
- **Cycle 107 — stress batch 9 ALL CLEAN (concurrency held + 4 edges graceful, no code change).** (e) TRUE-CONCURRENCY same-slot double-booking via 2 Ruby threads (separate connections, Queue barrier releasing both together) → T0=OK, T1=BLOCKED(RecordInvalid), appointments_landed=1 ✓ — the overlap validation held under concurrency. ⚠️ HONEST CAVEAT: Ruby's GVL limits true simultaneity so this does NOT prove the absence of a TOCTOU race; the overlap guard is APP-LEVEL (a validation query, no DB constraint), and a multi-Puma-worker / multi-process setup could in theory still race. RECOMMENDATION for Paul (defense-in-depth, NOT yet applied): add a Postgres EXCLUDE constraint on appointments (provider_id WITH =, tstzrange(start_time,end_time) WITH &&) — needs the btree_gist extension + a migration + handling cancelled/no_show rows; OR a pg advisory lock around the create path. Current single-worker setup makes a real race unlikely. Edges: re-cancelling an already-cancelled appt = idempotent; paying again on a fully-paid invoice = graceful credit (overpay, known); generate_estimate→void COT→estimate PDF still renders valid; reminders#send_reminder for a PAST appointment = graceful SendError (Twilio rejects, controller rescues). 
- **Cycle 106 — rig RECOVERED (~11min outage 14:18-14:29); NO leftovers (failed batch-8 curls never ran); batch 8 (b/c/d) done.** Cleanup inspection found ZERO leftover stress data (the backgrounded-curl-in-ssh failed before creating anything). Re-ran with sequential curls: (b) double generate_invoice on one COT → TWO invoices created (INV-2026-005549 + 005550), both valid — the "latest wins" behaviour the controller already documents; does NOT 500 or corrupt. DESIGN-NOTE for Paul: a double-click on "Generate invoice" makes a duplicate invoice + burns a sequence number; whether re-generate should create-new (current; intended for re-invoicing after more items complete) or redirect-to-existing is a product decision — a `redirect if COT already has a non-void invoice` guard is the small fix IF Paul wants idempotent. (c) intake PATCH /intake/new with empty body → HTTP 422 graceful (NOT 500), 0 junk patients created. (d) /search?q= empty → 200, q=2000-char → 200 (SQL-injection already covered by batch-5 sanitize_sql_like). STILL PENDING: (e) TRUE-concurrency same-slot double-booking — needs parallel processes (backgrounded-curl-in-ssh doesn't work); next cycle use a Ruby threads test w/ separate DB connections OR two parallel dev-PC curl processes.
- **Cycle 105 — INTERRUPTED by rig outage (SSH to 100.73.38.21 timed out).** Batch 8 (double-click generate_invoice idempotency + concurrent same-slot booking) was launched but the rig became unreachable mid-run (Tailscale/rig hiccup). ⚠️ POSSIBLE LEFTOVER STRESS DATA to clean once the rig is back: recent demo-patient Invoices (created last ~20 min, may have burnt sequence numbers), Appointments at 2027-04-20 09:00 (reason stressA/stressB), and a recent in_chair CourseOfTreatment for the demo patient. NEXT CYCLE MUST clean these FIRST. Also: the backgrounded-curl-inside-ssh approach was unreliable (no output) — use SEQUENTIAL rapid curls for the double-click idempotency test, and for true concurrency launch two parallel processes.
- **Cycle 104 — REAL BUG FOUND + FIXED: paying a VOIDED invoice was allowed + un-voided it.** register_payment! set status by paid-vs-total, so a payment on a voided invoice flipped status void→part_paid (resurrecting a cancelled invoice). FIX (two layers): payments_controller#create now rejects `if invoice.void?` with alert "This invoice has been voided — payments cannot be recorded against it"; Invoice#register_payment! now also `return if void?` (defense-in-depth, prevents the status corruption). VERIFIED: model — void invoice + register_payment!(5000) → status stays void, paid=0; controller — POST payment to void invoice → HTTP 303 rejected, 0 payments created, status void; REGRESSION — a normal invoice still takes a payment (part_paid/4000). NOTE: the double-accept-estimate idempotency test was inconclusive (Invoice.from_estimate doesn't exist; the real path is estimates#accept_and_invoice) — retest via curl next.
- **Cycle 103 — stress batch 6 (financial-integrity + load): mostly clean, 1 design-note for Paul, no code change.** (a) two payments (6000+4000) on one 10000 invoice → paid=10000 status=paid ✓ (sums correctly). (a2) OVERPAY: paying 15000 on a 10000 invoice → paid=15000, balance=-5000 (negative). No overpay cap. This is acceptable-by-design (a negative balance = a patient CREDIT; capping the payment would lose the record). DESIGN-NOTE for Paul: surface overpayment as a clear "credit" in the UI/statement someday; not a bug. (d) 120-line estimate → DocumentPdf paginates to ~5 pages in 338ms, valid (no crash/timeout). (e) reminder send to a BLANK-phone patient → ConfirmationService wraps the internal nil-phone error into ConfirmationService::SendError, which reminders#send_reminder rescues → graceful alert, NOT a 500 (a pre-check guard on blank phone would be cleaner but behaviour is safe). STILL TO RUN next: (b) generate_invoice/accept_and_invoice double-click idempotency (controller comment admits "latest wins"); (c) intake PATCH missing-required → friendly.
- **Cycle 102 — unicode-PDF fix extended to ReceiptPdf; whole class now CLOSED.** ReceiptPdf (payments#receipt) rendered @patient.full_name / billing_name / @pay.reference / practice fields with the built-in Windows-1252 font → same crash. Added winansi() + wrapped them. VERIFIED emoji/macron receipts render valid %PDF. Checked IntakePdf (patient_files#intake_pdf) — it ALREADY registers + uses the DejaVu TTF font, so it's NOT vulnerable (no change). NET: the non-Windows-1252-name PDF crash is now closed across ALL 4 renderers — DocumentPdf (invoice/estimate, c100), StatementPdf (c101), ReceiptPdf (c102), IntakePdf (already-safe). winansi() is duplicated in 3 services — a possible future refactor (shared concern/helper) but left inline to keep each PDF self-contained + the fix reversible per-file.
- **Cycle 101 — unicode-PDF fix extended to StatementPdf (same crash closed):** added the same winansi() sanitizer to app/services/statement_pdf.rb + wrapped practice_name, practice.address, account.billing_name/head.full_name, address_line1, scheme name, and the per-patient transaction rows (pt.full_name + display_phone, dependent line). VERIFIED: statements for emoji/macron/Cyrillic-named heads all render valid %PDF (were crashing). The invoice/estimate (DocumentPdf) + statement (StatementPdf) unicode crash is now fully closed. ⚠️ FOLLOW-UP: check the OTHER PDF services for the same built-in-font vulnerability — ReceiptPdf (payments#receipt, renders patient name) + IntakePdf (patient_files#intake_pdf) — apply winansi there too if they render user text with the default font.
- **Cycle 100 — REAL BUG FOUND + FIXED: unicode in a patient name crashed invoice/estimate PDFs.** A patient first/last name (or estimate line description) containing characters outside Windows-1252 (emoji 🦷, macron ā, Greek Ω, Cyrillic, …) raised Prawn::Errors::IncompatibleStringEncoding → 500 on the invoice/estimate PDF. Common SA Latin-1 accents (é, ë, ô) were already fine (in WinAnsi). FIX: tried Prawn fallback_fonts→DejaVu but that DOESN'T work with an AFM (built-in Helvetica) primary — the encoding error fires before glyph fallback. So added a `winansi()` sanitizer in document_pdf.rb (keeps Latin-1 as-is, I18n.transliterates the rest to ASCII where possible e.g. ā→a, drops unmappable e.g. emoji) applied to patient.full_name, scheme_name, provider_name, and line code+description. VERIFIED: emoji/macron/greek/cyrillic/latin1 names all now produce valid PDFs. Other batch-5 checks clean: audit-log no false log on a friendly-failed generate_invoice; search with %/_/'/\\ via sanitize_sql_like = no SQL error. ⚠️ FOLLOW-UP: StatementPdf has the SAME vulnerability (head.full_name / billing_name unicode → crash) — apply the same winansi there next. Also fixed my own test-harness FK bug (must null billing_accounts.head_patient_id before destroying a patient).
- **Cycle 99 — stress batch 4 ALL CLEAN (no breakages, no code change):**
- **Cycle 99 — stress batch 4 ALL CLEAN (no breakages, no code change):** (a) nil-field PDFs on REAL-shaped sparse records (patient w/ phone but no scheme, invoice 1 line + zero VAT + no provider_name) → invoice/statement/estimate/receipt PDFs all valid %PDF; (b) slot-finder dur=240 starting 16:00 (can't fit before 17:00 close) → gracefully pushes to a day it fits (returned 1); (c) malformed controller params via rig curl (Host:10.0.0.125): PATCH /treatment_items status=garbage → 303 guarded; POST generate_invoice on COT w/ no completed items → 303 friendly-fail; GET next_available w/ no provider → 200; statement from>to → 200; statement bad date strings → 200. Zero 500s. NOTE: malformed-param curls MUST run ON the rig (ssh) against localhost:3000 with Host:10.0.0.125 — running from the dev PC against localhost:3000 gives HTTP 000 (use 100.73.38.21:3000 for dev-PC GET checks).
- **Cycle 98 — stress batch 3 ALL CLEAN (no breakages, no code change):** double-booking same provider+overlapping window → BLOCKED ("This time slot conflicts with an existing appointment"); adjacent/back-to-back slot → allowed (no false-positive); end_time < start_time → BLOCKED ("End time must be after start time"); public host (wa.drchalitaleroux.co.za) hitting /dashboard + /patients + /audit-log → HTTP 401 (auth-walled, NOT 200 — confirms the earlier PHI-exposure fix still holds). Core booking guards + access control solid.

## Patient journey end-to-end (2026-06-06, session 5)
Closed the reception/clinical journey gaps + verified the whole flow unbroken (a brand-new test patient walked add→book→treatment-plan→estimate→print→convert→invoice→pay→statement, all ✓, then cleaned up). Fixes this session:
- **Estimate CREATION** was impossible from the UI (controller had only index/show). Added `estimates#create` (POST /patients/:patient_id/estimates) + "New estimate" button on the patient Estimates tab → blank draft → editor.
- **Treatment-plan (COT) CREATION** same gap → `courses_of_treatment#create` (POST /patients/:patient_id/courses_of_treatment, reuses an open one or creates) + "New treatment plan" button. The COT show page already had add-item + generate estimate/invoice.
- **🔴 COT controller 500 bug:** used the UNDEFINED `course_of_treatment_path` helper in 8 places (resources name singularizes oddly). Replaced all with `"/courses-of-treatment/#{id}"` URL strings → generate_invoice/estimate/add-item/chart all work now. (The singular path helper does NOT exist — always use the URL string or the plural `courses_of_treatment_path` for index.)
- **Estimate PROVIDER picker:** new estimates had no provider → converted invoices lost the dentist. Added a "Treating dentist" dropdown on EstimateShow + `estimates#update` (PATCH provider_name, UPPERCASED to match invoice/statement + HPCSA lookup). Carries through accept_and_invoice (cycle 22).
- **Statement reachable from patient:** added "View account & statement" link on the patient Account tab → /accounts/:id (was unreachable from the patient).
- Verified: estimate+invoice+statement+receipt PDFs all valid; accept→convert carries provider+lines+total; payment → part_paid/paid; add-patient lands on patient page; patient-flow status cards (overview) good.

## System know-how (what already exists — don't rebuild)
- **Digital INTAKE is already built INTO Ivory** (le-roux-repo), served publicly at `/intake/new` (intake.chalitaleroux.co.za). It is NOT a separate app. On submit, `app/services/intake_processor.rb` already: sync_patient! (creates/updates the Patient), sync_medical_history! (populates PatientMedicalHistory for at-a-glance), notify_practice!, file_to_patient_folder! (OneDrive "1. Patient Files"), emails the pack to info@drchalitaleroux.co.za. 3 forms (db/seeds/intake_forms.rb), mobile wizard (PublicIntake.jsx), tokenised WhatsApp link, branded PDF (intake_pdf.rb), PHI encrypted. Runbook: docs/INTAKE_GO_LIVE_RUNBOOK.md. → so "link intake into the system" is DONE; the remaining gap is RECEPTION-FACING: an at-a-glance "intake outstanding" list (sent/opened but not completed, esp. before an upcoming appt) + one-tap resend.
- **Sibling Dr Chalita projects** (D:\...\Claude\Projects\): DR CHALITA SEO (website/GBP/ads/content), DR CHALITA GOOGLE ADS, DR CHALITA GROWTH COMMAND CENTRE (has booking_joiner.py), Dr Chalita Filing (mailbox info@drchalitaleroux.co.za), Dr Chalita Transactions Check (bank reconciliation), Dental Receptionist Replacement CV'S (human-hire fallback), _shared (master CLAUDE.md + .env). Project root has IVORY_HOW_IT_WORKS.md + OVERNIGHT_BUILD_REPORT_*. Check these for know-how before building anything that might already exist.

## DIRECTIVE CHANGE (2026-06-06 ~12:25, Paul) — IMPLEMENT EVERYTHING + STRESS-TEST
Paul: don't slow down (cadence ≤3 min ALWAYS, never more); IMPLEMENT all the parked items now (he will review a REPORT drafted AFTER 7 Jun 13:00 and say which to reverse — so build reversibly via git); audit + stress-test everything; once nothing left to build, CONTINUOUSLY run stress tests + scenarios to find breakages + harden against future ones. STILL HARD CONSTRAINTS (do not override): never write Elixir (read-only); never reset logins; do NOT mass-mutate/merge/delete real patient data or backups (so DATA-cleanup parked items — 52 credits, VAT backfill, merge Morne Maartens+#2439, ambiguous phones — prepare scripts + document for Paul, do NOT execute irreversible mass mutations). Build code/feature items (reversible) first.

- **Cycle 97b — STRESS-TEST phase started + register_payment! hardened:** ran adversarial batches: ComplianceFilter.scrub(empty / 20k chars) no crash; NextAvailableSlotFinder dur=99999 (clamped to ≤240), dur=0/negative, nil provider (returns []) all graceful; StatementPdf for an account with NO invoices = valid PDF; payment amount=0 = no change. Two apparent PDF "crashes" were TEST ARTIFACTS (save!(validate:false) bypassed the Patient phone/ID requirement + the NOT-NULL auto-generated invoice_number — both are correct validations). FINDING+FIX: Invoice#register_payment! had no guard — a direct negative call drove paid_cents negative (NOT reachable in app: Payment validates amount_cents>0 + payments_controller guards amount<=0). Added defense-in-depth `return if amount_cents.to_i <= 0` (invoice.rb:47). Verified: -5000→paid stays 0, 0→0, +5000→5000/part_paid.
- **Cycle 97 — daily-huddle SKIPPED (redundant) + remaining big items DESIGN+PARKED (safe-or-park rule):**
  - **daily-huddle: SKIPPED honestly** — would duplicate the existing dashboard widgets (flow-board, ASAP, intake-outstanding, no-shows, lab-cases, reminders, stat cards). No value in a parallel summary.
  - **patients-559KB-trim: PARKED w/ design.** Cause: patient_list_props (patients_controller:391) embeds full medical_history (+ notes) per patient for ALL 2045 rows, so the Edit-from-list modal has it without a fetch — but only 1 patient is edited at a time, so ~2044 are wasted. Page is already fast (120ms server). DESIGN: add GET /patients/:id/medical_history (JSON), drop medical_history from patient_list_props, have PatientFormModal fetch it on open (loading state). Marginal benefit + Edit-modal regression risk → do deliberately, not rushed. NEEDS PAUL go-ahead.
  - **charting M/O/D/B/L surfaces: PARKED w/ design.** Odontogram is whole-tooth currently; surface-level (mesial/occlusal/distal/buccal/lingual) charting needs Dr Chalita's clinical conventions (which surfaces per procedure, colour coding existing/planned/completed) + a tooth_surfaces model. Clinical decision → NEEDS DR CHALITA.
  - **payment-allocation + collections-by-provider: PARKED w/ design.** Payments are account-level (invoice_id nil), so collections can't attribute to a dentist (cycle 76). DESIGN: a payment→invoice allocation step (allocate a payment across one/more invoices; store allocations) → then collections-by-provider groups by the allocated invoices' provider_name. Changes the payment model → PRODUCT DECISION for Paul.
  - **formal payment-plans: PARKED w/ design.** Part-payment already works (Invoice paid_cents/part_paid). DESIGN: a PaymentPlan model (invoice/account, instalment schedule, due dates, status) + a "set up a plan" UI + an overdue-instalment list. Whether the practice offers formal plans = PRODUCT DECISION for Paul.
  - **online self-booking / patient portal / Inertia-v2 upgrade: PARKED.** Big builds / risky upgrade — self-booking needs availability rules + public booking page + spam protection; portal needs auth + a patient-facing app; Inertia v2 is a dependency upgrade with regression risk. All NEED PAUL's product/architecture decision; not safe to force autonomously.
- **Cycle 96 — ASAP one-tap-offer shipped (completes cycle-61 ASAP feature):** Dashboard.jsx "Wants an earlier slot" list now has a wa.me "WhatsApp offer" link per candidate (pre-filled "an earlier appointment slot has just opened up... would you like it?") alongside the existing Call link. Reuses asap_list's patient_name + patient_phone (no backend change). Verified dashboard 200 + compiled into bundle. Reversible via git.
- **Cycle 95 — Review-request nudge shipped (coordinates with DR CHALITA SEO/GBP):** added google_review_url to PracticeSettings (migration 20260606131501) + editable in Settings practice form (practice_params permits it, index practice prop + Settings.jsx practiceField) + a patient-page "Request review" button (PatientShow.jsx, wa.me link with a pre-filled polite Google-review message, shown only when googleReviewUrl + patient.phone present). Used wa.me (reception sends manually) NOT an automated send — avoids the WhatsApp 24h-service-window/template limits. patients_controller show now passes google_review_url. VERIFIED: settings saves URL, patient prop carries it, /settings + /patients 200. Test URL cleared (blank for Paul/SEO to set the real GBP link). Reversible via git.
- **Cycle 94 — Practice-hours config (wired existing DoctorSchedule into the finder):** DoctorSchedule already holds per-weekday working hours (day_of_week/start_time/end_time/break_start/break_end/active, per provider_id; Mon-Fri 08:00-17:00, weekends inactive) — editable via Settings schedules. Refactored NextAvailableSlotFinder to READ it (load_schedules: provider-specific schedule preferred, else any active practice schedule per weekday; respects active days, open/close, AND break windows) instead of the hardcoded OPEN_HOUR=8/CLOSE_HOUR=17/weekend constants. Behaviour-preserving (same slots Mon 08:00/08:30, Tue 16:15; hours-map = Mon-Fri 8-17). Now editing the schedule (Saturday hours, lunch break, different times) flows into next-available automatically. Reversible via git.
- **Cycle 93 — Claim dependant-code shown on PDFs (mostly already built):** dependant_code ALREADY exists on SchemeMembershipPatient (the per-patient join, NOT scheme_memberships); the patient form captures it (PatientFormModal schema dependant_code) + patients_controller permits + saves it (smp.dependant_code). The only gap was the PDFs not showing it. FIXED: document_pdf.rb draw_patient_block now shows "Member X · Dependant YY" reading dep = SchemeMembershipPatient.find_by(scheme_membership_id:, patient_id:).dependant_code; statement_pdf reads the head's dependant_code from the join. Deleted a redundant migration I'd started (field already on the join). VERIFIED: dep lookup="01", invoice PDF +30 bytes with dep vs without (text rendered; grep can't see it — subsetted DejaVu font). Cleaned up test scheme data.
- **Cycle 92 — Billing-settings edit form shipped (was parked):** the Settings form edited PracticeSettings (contact) but the PDFs read PracticeBillingProfile (HPCSA/BHF/VAT/bank) which was rails-only. Added settings_controller billing_props + settings#update_billing (PATCH /settings/billing, billing_params permits hpcsa_number/bhf_practice_number/vat_number/vat_registered/company_reg/practitioner_name/practitioner_hpcsa_number/practitioner_bhf_number/bank_*) + route + a "Billing details (on invoices & statements)" Card in Settings.jsx PracticeTab (saveBilling + billingField). settings#index now passes the billing prop. VERIFIED: /settings 200, billing prop populated, PATCH company_reg round-trips, reverted to original (production data unchanged). Reversible via git. Note: PracticeSettings (contact) and PracticeBillingProfile (legal/billing) remain separate models — address/phone/email exist on both; the billing form edits the legal+bank fields.
- **Cycle 91 — Lab-case COT-page UI shipped (lab-case feature now COMPLETE):** CourseOfTreatmentShow.jsx — added a LabControl per treatment item: "Send to lab" (inline lab-name + due-back date inputs → PATCH /treatment_items/:id lab_name+lab_sent_on+lab_due_on), a "Returned" button (PATCH lab_returned_on=today), an "at lab (Lab) · due X" indigo badge on the description, + a setLab() router.patch helper; courses_of_treatment_controller show items prop now includes lab_name/lab_due_on/lab_returned_on. VERIFIED full lifecycle: send-to-lab → dashboard "Lab cases due back" shows it (count 1) → mark returned → drops off (count 0). Cleaned up. Reversible via git.
- **Cycle 90 — Lab-case tracking MVP built + invalid-token already-done + latent bug fixed:** (1) invalid-intake-token UX was ALREADY implemented (intakes_controller#render_invalid → PublicIntake invalid:true → "This link is no longer valid… contact the practice" page; cycle-85's "blank form" note was WRONG — corrected). (2) LAB-CASE MVP: migration 20260606123801 adds lab_name/lab_sent_on/lab_due_on/lab_returned_on to treatment_items (+ partial index); treatment_items_controller#update now permits those 4 fields; pages_controller dashboard lab_cases_due prop = TreatmentItem where lab_due_on present + lab_returned_on nil, joined to COT→patient + procedure_code; Dashboard.jsx "Lab cases due back" widget (indigo, due/overdue, patient link, procedure+tooth+lab). Verified: PATCH lab_due_on on a crown item → appears overdue on dashboard. (3) FIXED the latent undefined `course_of_treatment_path` helper in treatment_items_controller (2 uses, lines 22+58 — same bug as cycle 55 in a different file) → URL strings. FOLLOW-UP: COT-page UI to set lab dates (a "Send to lab / due-back" input on the treatment item; PATCH endpoint already accepts it).
- **Cycle 89 — Compliance-scrubber gaps CLOSED (was parked, now implemented + false-positive tested):** added 2 narrow rules to compliance_filter.rb — AFTER_HOURS_RULES: /see you (this|on|over)( the)? weekend/ + /(available|come in|book you) (this|on|over)( the)? (weekend|sat|sun)/ → "see you on the next working day (we're closed on weekends)"; SUPERLATIVE_RULES: /(the)? (best|finest|top|leading|greatest|premier) (dentist|dental( practice|care|clinic)?|practice|clinic)/ → "a trusted dental practice". TESTED: banned phrases now CAUGHT (see-you-this-weekend, Best-dentist-in-Roodepoort, book-you-on-the-weekend); legit phrases UNCHANGED (have-a-great-weekend, best-regards, works-best-for-you, within-24-hours, 24-hours-before) = zero false positives. Reversible via git.

## Best-in-market benchmark cycles (2026-06-06+, session 6) — to 7 Jun 13:00
Goal: make Ivory the best dental PMS on the market; each cycle benchmarks a NEW aspect (no repeats) vs Dentrix/Curve/tab32/Denticon/CareStack, implements the highest-value gap, records here.
- **Cycle 59 — Check-in/arrival flow:** added a "Patient flow today" board on the Dashboard (Dashboard.jsx, between checkout banners + stat cards) — groups today's appointments by journey status into Waiting (arrived) / In chair (in_consultation) / Ready for checkout (completed), with counts+names+times; appears only when patients are active. Best-in-class arrival = a live status-grouped flow board (not just diary pills). Verified data flows to the board.
- **Cycle 60 — Treatment-plan case acceptance:** added a per-visit SUBTOTAL footer (Medical / You-pay / Amount) on multi-visit estimates (EstimateShow.jsx, guarded by multiVisit) so each phase's cost is clear — benchmark says phased + per-phase 'what you pay' drives acceptance. PARKED bigger levers for Paul: priority phasing (urgent/needed/elective vs visit-based), accept-per-phase, financing/payment-plan options, diagnosis-led 'why' + annotated visuals.
- **Cycle 62 — Family/household account UX:** the account page (BillingAccountShow) members table now shows per-member NEXT APPOINTMENT + per-member BALANCE (Owes) + a Book/view link — a family-at-a-glance workspace (benchmark's biggest reception time-saver). billing_accounts_controller members prop adds next_appointment + outstanding per member. PARKED: book-next-family-member inline (reuse provider/slot), one family statement already covers all members.
- **Cycle 88 — Whitening-deposit flow verified (no build, matches PRACTICE_CONFIG_DRAFT §5):** prompt_builder:224 quotes whitening = 90 min, R7,800, R2,000 deposit to secure (= config §5 lines 132/153-154); whatsapp_service:862 sets booking notes "awaiting R2,000 whitening deposit"; whatsapp_service:111-120 tags the conversation deposit_proof_received when the patient sends proof media + has a whitening appointment; banking details sent in the deterministic first-touch whitening message. Quote + deposit + proof-tagging all consistent with the source of truth. Solid.
- **Cycle 87 — WhatsApp inbound→diary + emergency path verified live (no build):** the WhatsApp AI receptionist is operating in production — 1 real Conversation (#82 channel whatsapp, 8 messages, last 5 Jun 07:50, active), real future appointments in the diary (#391-393). Emergency triage: whatsapp_service#handle_urgent (line 1280) escalates to STAFF via send_flagged_alert("URGENT: Patient reported dental emergency via WhatsApp"); the PATIENT-facing emergency wording lives in the prompt and is already parked as 🔴 ER-escalation-wording (needs Dr Chalita clinical sign-off). Core product (WA inbound → AI → diary) works end-to-end with real patients.
- **Cycle 86 — Public-holiday/closure logic verified (no build):** wired into prompt_builder.rb (~line 99-108): AI told "OPEN Mon-Fri 8am-5pm excl. public holidays; CLOSED weekends + SA public holidays"; CONCRETE date lists for 2026 (1 Jan, 21 Mar, Good Fri 3 Apr, Family Day 6 Apr, Freedom 27 Apr, 1 May, 16 Jun, Women's 9-10 Aug, Heritage 24 Sep, 16 Dec, 25-26 Dec) + 2027; scripted refusal "that day is a public holiday and we'll be closed, I can book the next working day"; hard rule "NEVER create/propose/promise an appointment on a public holiday". Solid. MAINTENANCE NOTE for Paul: holiday dates hardcoded through 2027 — need a 2028+ refresh before then (annual upkeep).
- **Cycle 85 — Public intake token + webhook health verified (no build, security-relevant):** intake token = patient.signed_id(purpose: :intake, expires_in: 14.days); intakes_controller#show resolves via patient_from_token. Verified: valid token → /intake/<token> renders 200; INVALID token → 200 with a blank generic form (SAFE — a bad signed_id resolves to NO patient so zero PHI exposure; submit would create a new patient, not overwrite — degrades to generic self-registration); GET /webhooks/whatsapp health → 200. Minor UX note (low priority, not a bug/leak): invalid/expired token could show an explicit "link expired" message instead of a blank form.
- **Cycle 84 — Payment→receipt + search + statement-filter verified (no build):** payments_controller#create reads TOP-LEVEL params[:amount] + params[:method] (NOT nested payment[...] — matches InvoiceShow's router.post({amount, method})). Verified end-to-end: R115 invoice + POST amount=57.50&method=card → part_paid, paid R57.50, balance R57.50; /payments/:id/receipt → valid PDF (5170b); /search?q=demo 200; /accounts/:id/statement.pdf?from=&to= → valid PDF. All core money flows solid. (Initial test false-alarmed by using payment[amount] nesting — corrected.)
- **Cycle 83 — AI safety nets verified + compliance regex gap flagged (no build — patient-facing, needs Paul review):** ComplianceFilter.scrub wired (whatsapp_reply_job.rb:65) + works on critical phrases (24-hour→"Monday to Friday 8am-5pm" flagged after_hours; "bill medical aid directly"→"submit to your scheme for reimbursement" flagged medical_aid_direct; returns {text, flagged}). Guards present: verify_booking_response_consistency + BOOKING_CLAIM_PATTERNS (whatsapp_service ~190/204), ai_paused? (ws:81), handle_urgent (ws:1280). GAP FOUND (flagged for Paul, NOT auto-fixed — compliance regex needs human review to avoid false positives like "have a great weekend"/"best regards"): the scrubber's Rule 3 (weekend) + Rule 6 (superlatives) miss specific phrasings — "see you this weekend" and "Best dentist" slipped the patterns. AI prompt remains primary defense. Recommend NARROW additions: /see you (this|on)( the)? weekend/ and /\b(best|finest|top|leading)\s+(dentist|dental|practice|care)\b/ with false-positive tests.
- **Cycle 82 — Diary booking interactions verified (no build):** drag-to-reschedule + click-empty-to-book are wired (AppointmentCalendar.jsx: editable+eventDrop=handleEventDrop→PATCH /appointments/:id; selectable+dateClick=handleDateClick→booking modal with prefillStart/prefillProvider). Verified end-to-end: test appt A moved 14:00→15:00 (303); PATCH A onto an occupied 16:00 slot (overlaps appt B) → A STAYED at 15:00 = double-booking guard correctly blocks a clashing reschedule. Core booking UX + reschedule safety solid.
- **Cycle 81 — Empty-state scan + broad re-verify (honest, no build):** scanned empty-state messages across all pages — reasonable (several guide: "add tariff codes below", "Add one from the Edit form", "Run a SIDEXIS scan to match"; bare "No X yet" ones sit beside create buttons). No forced change. Broad re-verify GREEN: journey walk all 8 phases pass; 10 key pages (dashboard/diary/patients/estimates/invoices/accounts/reporting/recalls/reminders/COT) all 200, <75ms. System feature-complete for the core journey + best-in-market; remaining items parked for Paul.
- **Cycle 80 — Regression + billing-aging consistency (honest verify, no build):** the 3 billing-visibility changes (77 estimate-expiry, 78 invoice-overdue-age, 79 aged-debt) are safe — /estimates, estimate show, /invoices, invoice show, /reporting all 200. Confirmed StatementPdf#age_analysis (buckets Current≤30/31-60/61-90/91-120/120+) and Reporting aged_debt (Current/31-60/61-90/90+) use the SAME invoice_date day-thresholds → billing-aging is consistent across invoice badges + statement + reporting (statement just splits 90+ into 91-120/120+ for per-account detail). No discrepancy.
- **Cycle 79 — Aged-debt buckets on Reporting (small win):** reporting_controller now computes aged_debt = outstanding balance bucketed by invoice_date age into {current(0-30), d31_60, d61_90, d90plus} (rands) from Invoice.outstanding; Reporting.jsx shows a 4-card "Aged debt (outstanding by age)" row (90+ red, 31-90 amber). Verified: buckets reconcile exactly to total outstanding R40,417.94; 90+ = R18,627 (46% — collections priority). Pairs with cycle-78 invoice overdue-age badges.
- **Cycle 78 — Invoice overdue-age badge (small win):** outstanding invoices now show how old the debt is (presentational, no mutation). Invoices.jsx list date cell → "· Nd overdue" (amber 31-60, red 60+) when balance>0 & !void & status open/part_paid; InvoiceShow header → a "N days overdue" badge (amber/red) when outstanding & >30 days. Verified: invoice #5158 = 711 days old, R599.60 outstanding → renders red. NOTE FOR PAUL: real aged debt exists (invoices 700+ days outstanding) — a collections opportunity.
- **Cycle 77 — Estimate validity/expiry visibility (small win):** the `expired` estimate status exists but is never auto-set (data: 4229 draft, 0 expired). Added PRESENTATIONAL expiry flags (no status mutation): EstimateShow header now shows "Accepted" / "Valid until X · N days left" (amber ≤7d) / "Expired X — re-quote if proceeding" (red), computed from estimate.valid_until; Estimates.jsx list flags lapsed draft/sent rows with a red "· expired". Verified with a past-dated estimate (pages 200, props carry valid_until). Forward-looking: 30-day estimates flag as they age.
- **Cycle 76 — Collections-by-dentist (NOT feasible) + comms-history (verified):** collections-by-provider on Reporting is NOT feasible with current data — all this-month payments (R285,812) have invoice_id=nil (Payments are allocated at the ACCOUNT level, the Elixir way), so Payment.joins(:invoice).group(provider_name) returns {}. Honest finding tied to the payment-allocation model → PARKED for Paul (would need a payment→invoice allocation feature; production-by-dentist from cycle 65 still works). Patient COMMS-HISTORY verified reachable: PatientShow "Visits & Chats" tab lists patient.conversations as clickable Links to /conversations/:id (channel + message_count + date). No build needed.
- **Cycle 75 — Regression re-verify (GREEN) + lab-case tracking (parked design for Paul):** the cycle-74 COT provider_name migration is safe — journey walk all 8 phases pass; /courses-of-treatment, COT show, /reporting all 200. LAB-CASE TRACKING: none exists (only the TreatmentMacro.laboratory text field; treatment_items have no lab fields). Real gap but needs Paul's workflow buy-in → PARKED with a recommended MINIMAL MVP: add lab_due_on:date + lab_returned:boolean to treatment_items (or a small LabCase model: patient, treatment_item, description e.g. "CR #31 crown", lab_name, sent_on, due_back_on, returned_on, status sent/received/seated); a "Lab cases due" dashboard list (due-this-week + overdue, click-to-call + book-seat) reusing the ASAP/no-show list pattern; dentist flags a crown/bridge/denture item as out-at-lab with a due date at prep. OPEN QUESTIONS for Paul: which procedures are lab cases (auto from procedure-code category vs manual flag)? which labs (a lab list)? who manages it (dentist at prep vs reception)? ~1 cycle to build once confirmed.
- **Cycle 74 — COT→invoice provider gap CLOSED (was parked since cycle 55):** added provider_name to courses_of_treatment (migration 20260606104001) + a "Treating dentist" picker on CourseOfTreatmentShow (courses_of_treatment#set_provider, `patch :set_provider` member route, uppercased) + show props provider_name + providers list. Invoice.from_course + Estimate.from_course now pass `provider_name: cot.provider_name`. So a treatment plan's billing attributes to the right dentist in production-by-dentist + on the claim. Verified: COT(DR ELISKA)→invoice carried DR ELISKA; set_provider 303; COT page 200. The "unassigned" production-by-dentist case is resolved.
- **Cycle 73 — Treatment macros (verified comprehensive, no build):** TreatmentMacro is fully built — 20 macros (from "Dental Macro's.xlsx") covering everyday (CHECK UP AND CLEANING, FULL CHECK UP, PROPHY, WHITENING) + complex (bridges/implants/crowns/RCT/dentures/veneers/aligners); CourseOfTreatmentShow has a macro-picker button+list; apply_macro creates planned items per macro-item×qty. Verified: applying "3 UNIT BRIDGE" (16 macro-items) → 28 planned items in one click (303). Best-in-class; re-confirms cycle-55 fix covers apply_macro. No gap.
- **Cycle 72 — Next-available-slot finder (NEW FEATURE — booking-speed lever):** built app/services/next_available_slot_finder.rb (NextAvailableSlotFinder.call(provider:, duration_min:, limit:) — SAST Africa/Johannesburg, Mon–Fri 08:00–17:00, 15-min steps, skips weekends + provider on_leave_on? + booked appts (jumps past clashes), 30-day horizon) + GET /appointments/next_available (JSON {slots:[iso8601]}) + a "⚡ Find next available" button in AppointmentFormModal that fills Start (via dtLocal) with the 1st slot + offers the other 2 as chips + a "no slots" message. Verified: Eliska→[Mon 08:00, Mon 08:30, Tue 16:15]; Chalita(on leave until 31 Jul)→[]. Correctly models the maternity-leave reality. PARKED: business hours hardcoded (no practice-hours setting yet; would also handle lunch breaks).
- **Cycle 71 — Regression/quality pass (ALL GREEN):** after ~11 feature additions this session, smoke-tested all 22 main pages → all 200 (dashboard, diary, patients, patient show, estimates, estimate show, invoices, invoice show, accounts, account show, reporting, recalls, reminders, conversations + show, courses-of-treatment + show, settings, audit-log, imaging, mail, analytics). Re-ran the end-to-end journey walk (script/journey_walk.rb): all 8 phases pass (add→book→plan→estimate+provider→print→convert→pay→statement), cleaned up. No regressions. Estimate PDF now 8965b (was 8788) confirming the cycle-67 treating-provider HPCSA line is live on estimates too.
- **Cycle 70 — Dashboard performance / N+1 fix:** measured dashboard ~25ms warm (fast even with all the new widgets). Fixed the intake-outstanding N+1 (was 1 + 2×N form_submissions queries per upcoming appt) → extracted to pages_controller#intake_outstanding_list = 2 queries (appointments + one FormSubmission.pluck sweep, aggregated in Ruby: completed_pids set + latest_pending status per patient). Behaviour-preserving (verified identical 15-patient output). Future-proofs a busy day.
- **Cycle 69 — No-show management:** no_show status existed (diary) but no recovery flow. Added (1) a dashboard "Recent no-shows — rebook them" list (pages_controller no_shows_to_rebook prop = no_show appts last 30 days, patient + click-to-call + Rebook link) and (2) a "No-show rate (90d)" KPI on Reporting (reporting_controller no_show_rate = no_show / past appts in last 90 days; green ≤5% / amber ≤10% / red >10%). Verified: test no-show surfaced; rate computed 2.2%. Benchmark: speed is the lever (78% rebook with whoever responds first).
- **Cycle 68 — Messaging cadence + charting (honest verify, no forced build):** appointment CONFIRMATION/reminder cadence is already STRONG — reminders_controller#index = the unconfirmed-upcoming list reception works (pending vs confirmed stats + send WhatsApp/voice via ConfirmationService); diary colour-codes confirmed=green/pending_confirmation=white; dashboard surfaces reminders. CHARTING: verified the chart→plan linkage works end-to-end (POST chart_quick_add → ChartingService.add_from_chart → ToothChartEntry tooth 11 extraction_planned + a planned TreatmentItem on the COT; 303). Confirms the cycle-55 fix held (chart_quick_add was one of the 8 broken course_of_treatment_path actions). Odontogram.jsx = whole-tooth conditions + click-to-chart (no M/O/D/B/L surfaces — clinical, parked). Both best-in-class; cleaned up test data.
- **Cycle 67 — Medical-aid claim assistance (extended invoice/estimate PDF):** practice does NOT bill schemes — patients self-claim, so the invoice must be claim-ready. It already had practice HPCSA + BHF + VAT + scheme+member + per-line ICD-10 + tooth#s + "submit to your scheme" note. GAP: it showed only the PRACTICE hpcsa, not the TREATING DENTIST's individual HPCSA (the scheme needs that, and the invoice knows provider_name). Added a "Treating provider: {name} · HPCSA: {number}" line to DocumentPdf.draw_header, reusing StatementPdf::PROVIDER_HPCSA (Chalita DP0118702 / Eliska DP0122343 / Theo DP0126659 / Anneze / Eric), fallback = practice hpcsa. Verified PDF valid + lookup resolves. PARKED: dependant code (member number shown; dependant-code not in the data model).
- **Cycle 66 — Perceived speed (good) → new-staff onboarding:** measured server timings — ALL pages fast (<130ms: dashboard 87, patients 120, diary 33, reporting 18, invoices 19, accounts 30). No slowness. PARKED for Paul (risky to do autonomously): (1) Inertia is v1 (`@inertiajs/react ^1.0.0`) so native `<Link prefetch>` needs a v2 upgrade → instant nav; (2) /patients ships 559KB (all 2045 patients incl. preloaded medical_history for the row-detail panel + client-side search) → lazy-load medical_history on row-select to cut it. Shipped a SAFE onboarding win: a dismissible "New here? Quick start" card on the Dashboard (Dashboard.jsx, localStorage key ivory_quickstart_dismissed) with the 5 core actions (Book/Add patient/Take payment/Work recalls/Statements).
- **Cycle 65 — Provider productivity (pivoted from payment-plans):** payment-plans — part-payment already works (Invoice paid_cents/balance/part_paid via register_payment!); a FORMAL installment-plan model is a PRODUCT DECISION → parked for Paul. Built per-dentist production instead: reporting_controller production_by_provider prop (invoices this month grouped by provider_name → {provider, production, invoices}, sorted desc) + a "Production by dentist (month)" table on Reporting.jsx. Real data: Eliska R279,843 (30), Chalita R30,583 (3), unassigned R85 (1 = the COT→invoice nil-provider gap, now visible). Leverages the provider-distinction work. PARKED: collections-by-provider (needs payments→invoice join), formal payment plans.
- **Cycle 64 — Recall reactivation (made the read-only recalls page actionable):** Recalls.jsx now lets reception WORK the list — click-to-call, one-tap Contacted/Booked status, overdue badge, patient link. Added recalls#update + `patch recalls/:id` route (status in Recall::STATUSES due/contacted/booked/done/cancelled); recalls_controller index prop adds patient_id+patient_phone. PARKED: post-visit Google-review-request nudge (bigger; coordinate with DR CHALITA SEO who owns GBP/reviews) + SMS/WhatsApp recall automation (two-way 'reply YES to book').
- **Cycle 63 — Intake outstanding (extended existing intake, did NOT rebuild):** added a reception-facing "Intake outstanding before visit" dashboard widget (pages_controller intake_outstanding prop = appts in next 7 days whose patient has NO completed FormSubmission; shows status not-sent/sent/opened) + a one-tap "Send intake" button reusing the existing POST /patients/:id/send-intake → IntakeDispatch. Surfaced 15 real upcoming patients with no intake. Reception chases before the visit so the chart is ready on arrival.
- **Cycle 61 — Cancellation-fill / ASAP waitlist (NEW FEATURE):** appointments now have an `asap` boolean (migration 20260606090001, partial index where asap=true). Booking modal (AppointmentFormModal) has a "Patient wants an earlier slot" checkbox; appointments_controller permits+saves :asap; Dashboard shows a "Wants an earlier slot" list (pages_controller asap_list prop = upcoming asap appts, patient + click-to-call display_phone). Reception fills gaps when a slot opens — cuts empty chairs. Verified end-to-end. NEXT to build (parked): one-tap "offer this slot" SMS/WhatsApp to ASAP candidates + preferences (days/times) on the flag.

## Continuous-improvement loop (2026-06-05, session 4) — ongoing
Running benchmark-driven cycles (scenario → Perplexity best-practice → implement → browser-verify → log in todo ledger). Each cycle is visible to Paul as a completed task. Done so far:
- **Provider distinction:** the treating dentist is `ACCOUNTITEMS.PROVIDER` (a NAME string: "DR CHALITA LE ROUX"; `PROVIDERLINK` is the id; `PROVIDERS` table maps id→dentist + holds each one's HPCSA — Chalita DP0118702, Eliska DP0122343). Added `provider_name` to invoices+estimates (migration), re-imported the ledger w/ provider, shown on account ledger + tax-invoice PDF + invoice page. The ledger import now maps account_cl→REAL code via `internal.json` (NOT computed).
- **Tariff fees:** the first import used the LATEST billed AMOUNT → caught multi-tooth lines → outliers (crown R27.5k). Fixed: `script/fix_tariff_fees.rb` recomputes 190 fees to the MODE of amount/qty (per-unit). Re-run if re-importing tariffs.
- **Statement (`StatementPdf`):** rebuilt to match the REAL Elixir statement (B0117 sample): dependent grouping (name+DOB+cell), provider line w/ BHF+HPCSA, `code × qty  description` rows w/ Amount/Credit/Due, Place of service [11], ICD-10, medical-fund reference, age analysis (Current/30/60/90/90+), bank, thank-you. PROVIDER_HPCSA lookup embedded.
- **Demo data:** `script/seed_demo_data.rb` — 4 families (DEMO001-004) w/ dependants, mixed-provider/paid-state invoices, varied estimates. Tagged `[demo]`. FK-safe clear order: account invoices+estimates+account_patients+head_patient_id BEFORE patients.
- **Booking:** added "Save & book another" to AppointmentFormModal. **Search:** added account-code matching (W0046→family) + national phone. **Estimate line editor**, **account transactions+statement+date-range** all built earlier this session.
- **Receipt:** `ReceiptPdf` (payment receipt) + route `GET /payments/:id/receipt` + InvoiceShow link — done.
- **Deep audits (cycles 22-24):** (a) FIXED `Estimate#accept_and_invoice!` was dropping `provider_name` → converted invoices lost the dentist; now carried through (verified: provider+lines+total+VAT all match). (b) Double-booking guard (`Appointment#no_overlapping_appointments`) verified solid at runtime — per-provider, excludes cancelled+self, back-to-back allowed, cancel frees the slot. (c) Statement/ledger MATH is correct — reconciles per-account exactly (ledger closing = billed−paid = age-analysis). Practice-wide there are **52 accounts holding small overpayment CREDITS totalling R87,638.76** (avg tiny, e.g. R22-R475) — legitimate, not a bug. The right "what patients owe" figure = **sum of unpaid invoice portions** (`Invoice.where(status: %w[open part_paid]).sum("total_cents-paid_cents")`), which the dashboard + AR use; do NOT use `billed − all_payments` practice-wide (it nets credits and goes negative). **Open Q for Paul:** are those 52 credit balances to be refunded / carried / written back? (d) Payment allocation (cycle 27) verified correct at runtime — partial→part_paid, full→paid, overpay→paid+negative balance (= the credits mechanism). (e) VAT audit (cycle 28): **header VAT is 100% correct** across all 2749 invoices (subtotal+vat=total AND line_totals sum to total — 0 bad). BUT **imported invoice lines have no per-line `vat_cents`** (20,728/20,750 lines are 0/nil; only app-created lines via accept_and_invoice/demo carry it). Header VAT is what's displayed + VAT-compliance-relevant → NO visible bug; per-line VAT is latent/undisplayed. Safe backfill if ever needed: proportionally allocate the correct header vat across lines (guarantees sum match) OR re-import `ACCOUNTITEMS.VATVALUE` per line. Low priority.

**🔴→🟢 BACKUPS (cycle 40, CRITICAL):** the nightly encrypted backup (`script/rig_backup.sh`, cron 23:30 → `/opt/ivory/backups/ivory-*.sql.gz.enc`, AES-256, pass in `/root/.ivory_backup_pass`) was SILENTLY FAILING since Jun-4 — the script had been saved with **CRLF line endings** (Windows edit), so `set -euo pipefail` errored ("set: pipefail: invalid option name") and produced NO file. Last good backup was Jun-3; 2 days unprotected. FIXED on the rig (`sed -i 's/\r$//'`), ran a fresh backup (9.2 MB, Jun-5), and VERIFIED restorable (decrypts→gunzips→valid pg_dump, 72 tables). LESSON: keep `script/*.sh` as **LF** — a CRLF shell script breaks on the rig. If you edit rig scripts on Windows, strip CR before deploying. Restore cmd is in the script header.

**Compliance guard VERIFIED (cycle 44):** `ComplianceFilter.scrub` (app/services/compliance_filter.rb) is wired into EVERY outbound AI WhatsApp reply at `app/jobs/whatsapp_reply_job.rb:65`, and proven to catch+rewrite banned phrases (after_hours→hours, superlatives→trusted/well-priced, medical_aid_direct→submit-for-reimbursement) + log each hit. NOTE: it FAILS OPEN (rescue at compliance_filter.rb:163 passes the original through on scrub error) — Paul to decide if hard-compliance wants fail-closed. AI handoff also verified (cycle 43): manual reply → `pause_ai!` 4h; inbound webhook respects `ai_paused?` at whatsapp_service.rb:81 (saves inbound, skips AI). PHI-at-rest verified encrypted (cycle 39): form_submissions.data + patients.id_number ciphertext, 0 plaintext stragglers.

**Verification note:** Playwright runs from Paul's PC over Tailscale; when Tailscale drops to a slow DERP relay, Chromium `networkidle`/`load` can time out at 25-35s though the app serves 200 in <2s — use a **90s timeout** (`waitUntil:'load'`) to load over the degraded link. Server-side `curl` (rig localhost, Host: 10.0.0.125) is the fast fallback.

## Account codes — SOURCE OF TRUTH (2026-06-05)
The REAL Elixir account code (e.g. `W0046`, `A0001`, surname-initial + per-initial number) is stored in **`ACCOUNTS.INTERNAL`** (NOT PRIORFILE/FILEREFER/IDENTIFIER, which are empty/internal). It also appears in `EXPRESSAPPOINTMENTS.CAPTION` as `[W0046]`. Ivory's earlier computed A0001-sequential codes were WRONG; replaced with the real `INTERNAL` codes via `script/apply_real_account_codes.rb` (maps account_cl→INTERNAL from `internal.json`, applied through each account's patients; full-TMP two-pass for the unique index). 1774/1790 matched; 25 unmatched left blank. ANY future patient/account re-import MUST pull `ACCOUNTS.INTERNAL` for the code.

## Done (2026-06-05 session 3 — journey hardening)
- **Browser verification harness** `script/browser_smoke.mjs` (Playwright on Paul's PC → rig over Tailscale). Frontend deploy = source → `bin/vite build` → restart web (reload manifest) → browser smoke. Catches client crashes curl misses. See `meta/verification.md`, non-negotiable #14. Hourly `script/healthcheck.sh` via rig cron → `/opt/ivory/health/`.
- **SSL/CSRF fixes (LAN+Tailscale, http):** HSTS disabled (`ssl_options={hsts:false}`); CSRF origin-check off + CSRF skipped on Tailscale (`csrf_exempt_network?` = LAN ∪ 100.64/10); `after_action :downgrade_redirect_scheme_for_private_network` (redirect_to emitted https → ERR_SSL on http → broke create-patient). assume_ssl KEPT (webhook needs https original_url).
- **VAT → 15% inclusive** (procedure codes + estimates), confirmed from ACCOUNTITEMS (99% lines VATRATE=15, inclusive). **Tariff catalogue** 190 codes w/ real latest fees (import_elixir_tariffs). Tariff **add/inline-edit/bulk-uplift** UI.
- **Ledger import:** 2747 invoices + 4326 payments (allocated oldest-first), R21.8k outstanding (import_elixir_ledger).
- **Phone:** `Patient.canonical_phone` (shared by model + PatientRegistrationService) → +27 E.164; `display_phone` → national "072 123 4567"; 1859 re-normalised.
- **Patient list** shows account_code (was "P#id"). **Estimate title** = treatment (strip "Cost Acceptance"/"Treatment Planning").
- **Diary:** click-select + Ctrl+C/X/V (cut=move) + right-click status; **cancel=grey & KEEP** (removed `.where.not(status: :cancelled)`, set_status allows cancelled).
- **Estimate line editor** (EstimateLinesController + EstimateShow): search tariff code → add w/ tooth/qty, fee auto-fills, recalc, remove — verified end-to-end.
- Queued: backfill estimate lines from ESTIMATEITEMS; account transactions→tax-invoice-per-date + statement-date-range; provider (Chalita/Eliska) distinction; patient hard-delete notification FK cascade.

## Done (2026-06-05 session 2)
- **CRITICAL FIX:** LAN writes were all 422 (app `assume_ssl`/`force_ssl` for the CF tunnel vs the LAN's plain HTTP → CSRF origin mismatch). Fixed: `skip_before_action :verify_authenticity_token, if: :on_trusted_lan?` in ApplicationController. All diary writes verified (303).
- **Diary UX (editable working diary):** right-click status menu (DiaryContextMenu.jsx → set_status), drag-to-move, Ctrl+C/Ctrl+V copy-paste. Editable appts = 103 (61 future), matched to real patients.
- **Patient import:** 2,493 patients, 1,790 A-coded billing accounts (A0001, A0002…, surname-alphabetical, account-level). Patients list now ordered by account number (patients_controller index, left_join billing_accounts). Scripts: import_elixir_patients.rb (+ parse_elixir_patients.py). ~30 placeholder patients remain on appts without an Elixir name-match; ~69 old patients have no account code.
- **Estimates:** 4,226 editable `Estimate` records under patients (header-level: total + treatment text, status=draft; NO line items yet). Scripts: import_elixir_estimates.rb (+ parse_elixir_estimates.py), also mirrored in ElixirEstimateSnapshot.

## Done (session 1)
- **Editable diary**: 103 Ivory appointments + 68 block-outs imported from Elixir (61 future), fully editable. Patient name-match ~35% → 68 placeholder patients (synthetic ids, pending real demographics).
- **Read-only diary mirror** (`ElixirDiarySnapshot`) also exists — superseded by the editable import for the test phase.
- **Live data access** proven: copy-MDLDATA.FDB-to-OneDrive → read with masterkey. One-command refresh: `bash le-roux-repo/script/refresh_elixir_mirror.sh`.
- **Reception diary** reachable on practice LAN (no password) + drag-to-move shipped; remote needs password.
- **Tailscale** persistent (key expiry off, SSH check→accept) across laptop/rig/reception-pc.
- **Knowledge base** consolidated into `system/` (this directory) — single source of truth.

## Next (in order)
1. **Patient import** from Elixir `ACCOUNTS`+`MEDICAL` (real names/phone/id/email/medical-aid/account_code) → replaces placeholder patients, enables clean account-code matching.
2. **Estimates** (`ESTIMATES`+`ESTIMATEITEMS`) under each patient.
3. **Diary UX**: right-click status menu (PATCH `set_status`), drag-to-move, Ctrl+C/Ctrl+V copy/paste.
4. **Daily memory consolidation** job (see `meta/daily-consolidation.md`).

## AI-estimate/invoice composer + continuous-improvement research (2026-06-07 ~14:55)

- ✅ **NEW INVOICE flow shipped** — patient → Invoices tab → "+ New invoice" → opens the editor with the AI box → describe → AI fills codes → adjust → "Create invoice" (relabelled accept button). Reuses the proven estimate editor (invoices stay immutable; the draft estimate is the editable staging). Intent persisted via `?intent=invoice` URL param → `as_invoice` prop (verified true/false in Inertia page data; throwaway drafts cleaned up). Files: estimates_controller#create (intent) + render_inertia (as_invoice prop); EstimateShow.jsx (button relabel Create invoice/Invoiced); PatientShow.jsx (New invoice button). SAFE-deployed (vite build confirmed BEFORE restart), live 200.
- 📚 **Perplexity research — how serious teams continuously improve systems like this** (Paul asked). (1) Healthcare-SaaS CI = a CLOSED-LOOP operating system: instrument usage+reliability (logs/metrics/traces/audits), set SLOs + error budgets (e.g. 99.9% chart-save <Xms; <0.1% failed bookings), ship small behind feature flags, measure, feed back from support-ticket mining + user advisory boards + session replay + A/B tests + competitive benchmarking; cadence daily/weekly telemetry → biweekly/monthly experiments+customer review → quarterly strategy; methods Lean/Kaizen/PDCA/Six Sigma/Model-for-Improvement. (2) For the AI code-composer specifically: a measure→learn-from-edits→update-prompt/RAG→regression-eval loop. KEY METRICS: exact-code accuracy (codes present in top-K), per-code precision/recall, TOP-1 ACCEPTANCE RATE, **EDIT RATE** (codes added/removed/modified by staff), time-to-completion, error mix (under- vs over-coding); sliced by treatment/visit type. Golden dataset from historical FINAL-BILLED estimates; LLM-as-judge + regression evals to prevent regressions; ground in practice history (few-shot/RAG). → NEXT LOOP DIRECTION: build a composer EVAL HARNESS (golden prompts→expected codes, e.g. 'root canal 26 ⇒ 8339+8340×2') run on every prompt change + capture EDIT-RATE (flag AI-added lines, log keep/remove) as the live feedback signal. This turns 'best-in-market' into something measured + regression-proof. ✅ EVAL HARNESS BUILT 2026-06-07 ~14:58: script/evals/composer_evals.rb — golden set of 8 prompt→must-include-code cases from the recipes (root canal 26⇒8339+8340×2+setup; crown 11⇒8409/8560/8570/8146; simple extraction 46⇒8201; whitening⇒8159/8308×2/8304 & NOT 8145; filling 36⇒8367-70; check+clean⇒8159+8101/8104; two crowns 21&11⇒8409 per-tooth; wisdom 48⇒8937+8220). BASELINE 8/8 (100%) GREEN. Run on every prompt/grounding change as the regression gate; add each new failure as a case. Also: NEW SKILL self-improve-loop-pro built (~/.claude/skills/) — base loop + serious-team closed-loop (SLOs/error-budgets, guardrail invariants, RICE selection, ship-behind-flags, REGRESSION GATE = tests+eval harness, edit-rate feedback). PAUL 2026-06-07 ~15:00: run the PRO loop on Ivory until 19:00 SAST, cadence ≤2min (ScheduleWakeup ≤120s). ── PRO-LOOP DONE-LEDGER ── [C1 ~14:57] composer EVAL HARNESS built (script/evals/composer_evals.rb, 8 golden cases) → baseline 8/8 GREEN; metric=composer top-K must-include accuracy 100%. [C2 ~15:05] EDIT-RATE capture: migration 20260607150001 add estimate_lines.source (default 'manual', additive/reversible) + estimates#ai_compose sets source:'ai'; e2e 9/9 AI lines tagged; REGRESSION GATE eval 8/8 GREEN (composer unchanged); schema.rb pulled; metric=AI-line survival/override rate now measurable (keep-vs-remove of source='ai' lines at acceptance). Guardrails respected (additive, USER_AUTH off, no real-data mutation — throwaway on 2985 cleaned). [C3 ~15:13] ⭐ ESTIMATES TRACKER (Paul's ask) — /estimates is now a colour-coded lifecycle tracker: EstimatesController#index computes status_colour (GREEN=accepted/done, BLUE=not-accepted+future booked appt [one batched Appointment query, no N+1], YELLOW=outstanding) + columns account_code/date_sent/details/provider/value + stats counts; Estimates.jsx rewritten as tracker (colour pills, filter tabs All/Outstanding/Booked/Done, legend, your spreadsheet columns). SAFE-deployed (vite confirmed before restart). VERIFIED live 200, real colour distribution 452 yellow / 46 blue / 2 green. Mirrors the manual 'Estimates listing.xlsx' as a live auto-logged view. FOLLOW-UPS (backlog): last-follow-up + update-note + does-patient-know columns need tracking fields; refine GREEN to also include completed-COT (not just accepted). [C4 ~15:22] ⭐ DENTAL CHART TAB on PatientShow (Paul's ask) — read the practice colour spec (file 8: BLACK=existing/done, RED=work-to-do) first. PatientsController#show now ships tooth_chart_for(patient): RED 'needs_work' = planned treatment_items + extraction_planned ToothChartEntry; BLACK 'done' = completed items + existing restorations (filling/crown/bridge/implant/root_canal); needs_work wins. Odontogram.jsx given 2 ADDITIVE styles (needs_work=red, done=black) — COT page unaffected. PatientShow.jsx new 'Dental Chart' tab (Grid3x3) reusing Odontogram + a red/black/white legend. SAFE-deployed (vite confirmed before restart). VERIFIED: patients 7/33 + chart render 200; tooth_chart_for(2985)={11:done}; REGRESSION COT-79 (also Odontogram) + estimates tracker both 200. Follow-up: make red teeth clickable→prefill AI box (backlog d); surface caries-without-plan. [C5 ~15:30] EXPANDED eval golden set (8→13: +bridge/implant/veneer/partial-denture/pulpotomy + must_not 8202 on extraction) — which SURFACED a real robustness bug: AiService#compose_treatment_lines only rescued Anthropic/Faraday/JSON, NOT the wrapped AiService::Error from create_message → an API error would 500 the live ai_compose. FIXED → `rescue StandardError` returns {lines:[],error:} gracefully + logs. 🔴🔴 CRITICAL FINDING FOR PAUL: the ANTHROPIC_API_KEY has HIT ITS USAGE/SPEND LIMIT — direct API call returns 400 'You have reached your specified API usage limits. You will regain access on 2026-07-01 at 00:00 UTC.' So ALL AI features are DOWN until the cap is raised (Anthropic console → spend/usage limits) or 1 Jul: the estimate/invoice AI composer, plus likely WhatsApp-AI replies, voice agent, intent classification. Good news: composer now DEGRADES GRACEFULLY (manual entry still works, no 500). Eval reads 0/13 ONLY because the API is capped (NOT a regression — same calls were 8/8 at ~14:57). ACTION FOR PAUL: raise the Anthropic usage limit / add credit to restore AI. LOOP PIVOTS to NON-AI work (tracker follow-ups, PMS polish, reliability, dental-chart UX) until API restored; re-run composer_evals.rb to reconfirm 8/8+ once it is. [C6 ~15:35] TRACKER follow-ups (non-AI) — GREEN now also = treatment actually done (linked COT's items ALL completed, batched via TreatmentItem group counts, no N+1), not just status=accepted; added 'Last activity' (updated_at) column + sort most-recent-first. Verified live 200; green 2→3 on real data (one completed-COT estimate now correctly green); last_activity present. Display-only, additive, no regression. [C7 ~15:41] TRACKER CSV EXPORT (non-AI) — EstimatesController#index now respond_to html(inertia)+csv (require 'csv', CSV.generate, send_data estimates-tracker-<date>.csv); Estimates.jsx 'Export CSV' button (→ /estimates.csv). Columns Status/Number/Patient/Account/Date sent/Last activity/Treatment/Dr/Value — mirrors the manual Estimates listing.xlsx so reception keeps their spreadsheet habit. Verified: /estimates.csv 200 text/csv with real rows; /estimates html 200 (no regression). API still capped (rechecked 'usage limits'). [C8 ~15:45] RELIABILITY/OBSERVABILITY (non-AI) — enriched /health (HealthController): now reports db_latency_ms (timed SELECT 1) + queue_pending (SolidQueue::Job unfinished count, nil-safe) and returns DEGRADED/503 if db down OR db_latency≥2000ms OR queue_pending≥500 (jobs not draining = WhatsApp/reminders stuck). Verified live: {status:ok, db:true, db_latency_ms:10.1, queue_pending:5}. An external uptime monitor now sees real readiness. NOTE: backups live HOST-side /opt/ivory/backups/ivory-*.sql.gz.enc (newest 2026-06-07 06:49) — OUTSIDE the container mount, so /health can't stat them; NEXT add backup-freshness via a marker the backup writes + /health reads it. Additive, no regression. [C9 ~15:51] BACKUP-FRESHNESS in /health (non-AI) — rig_backup.sh now writes ops/last_backup.json on each successful run; /health reports backup_age_hours + DEGRADED/503 if >36h (silently-failing nightly backup pages the monitor). GOTCHA solved: /rails/tmp is a NAMED VOLUME (not host repo tmp) so the marker uses ops/ which IS in the `.:/rails` bind mount — container confirmed reads it. Backfilled from newest dump. Verified live: {status:ok, db_latency_ms:10.3, queue_pending:5, backup_age_hours:7.0}. /health now a COMPLETE readiness probe (db up + latency + queue + backup). Additive, no regression. [C10 ~15:56] DATA-INTEGRITY sweep (live_audit.rb, read-only) — CLEAN on all money/booking invariants (overpay, status/void mismatch, negative money, line!=header drift, double-bookings, bad procedure refs all 0). One ⚠️: 🔴 QUESTION FOR PAUL — payment 8686 is a GENUINE ORPHAN: R50.00 cash, kind=payment, invoice_id/billing_account_id/patient_id ALL nil, created 2026-06-06 13:38 (yesterday), not reversed; it FAILS the must_have_an_owner validation (added cycle 1) so it's a PRE-validation straggler (guard now blocks new ones). Real money → NOT deleting/guessing an owner. ACTION FOR PAUL: whose R50 cash is this? attribute it to a patient/account, or confirm it's a test row to remove. (Note: as kind=payment unreversed it sits in the `inward` scope so it's R50 of money-in attached to nothing.) No mutation this cycle. [C11 ~16:02] LOCK /health with a request spec (spec/requests/health_spec.rb, 2 examples 0 failures) — asserts the readiness payload keys (db/db_latency_ms/queue_pending/backup_age_hours/time) + status↔HTTP-code contract + no-auth reachability; deliberately does NOT mutate the shared ops/last_backup.json marker. SURFACED+FIXED real SCHEMA DRIFT: rig db/schema.rb was stale at version 2026_06_07_130001 (missing C2's add_source_to_estimate_lines 150001 though the column existed in the DB) — test DB wouldn't sync via schema:load; resolved by migrating the test DB (RAILS_ENV=test + TEST_DATABASE_URL) AND regenerating schema.rb via db:schema:dump → now 2026_06_07_150001 on rig+local (CI schema-drift guard will pass). Lesson: after a rig db:migrate, run db:schema:dump + pull (the C2 migrate didn't refresh schema.rb). [C12 ~16:13] DENTAL CHART tooltip (non-AI UX) — PatientsController#tooth_chart_detail_for returns {tooth: 'Crown; RCT'} from PLANNED treatment_items' procedure descriptions; Odontogram.jsx given an OPTIONAL `details` prop appended to each tooth's title (COT page that uses Odontogram passes no details → unaffected); PatientShow passes it on the Dental Chart tab. SAFE-deployed (vite before restart). Verified: patients 7/33 200, tooth_chart_detail prop shipped, COT-79 regression 200. NOTE: detail map currently sparse — 0 planned treatment_items have a tooth_number in live data (practice still charts planned work on paper; this is what the AI chart-reader is for) so red teeth come from extraction_planned only; tooltip is correct + ready as digital planned-charting grows. Additive, no regression. [C13 ~16:20] ESTIMATES TRACKER PIPELINE VALUE (non-AI) — EstimatesController#index stats now include outstanding_value/booked_value/done_value (sum of each colour's estimate totals); Estimates.jsx filter chips show compact rand (randK: R1.2m/R450k). Reception now sees the sales pipeline at a glance. Verified live 200: outstanding R6,419,774.83 / booked R943,655.18 / done R14,969.04 — i.e. ~R6.4m of un-accepted estimates outstanding. WATCHDOG (Paul ask): CronCreate job 8d000d72 every 10min (:04..:54) checks build-status.md mtime; if >12min stale & <19:00 restarts the loop (session-only — durable-to-disk not honored here, so it catches loop-chain death but not full-session close; offered a rig/PC OS-cron for true durability). Additive, no regression. [C14 ~16:30] DASHBOARD estimate-pipeline KPI (non-AI) — pages#dashboard stats + estimates_awaiting/estimates_pipeline; Dashboard.jsx new clickable 'Estimate pipeline' StatCard (TrendingUp, →/estimates), grid widened xl:grid-cols-4→5. VERIFY-CAUGHT a real refinement: first cut counted ALL draft/sent estimates = 4234/R72.8m (misleading — almost all imported estimates are status 'draft' forever, years of history); SCOPED to last 90 days → now R3.37m across 253 awaiting = the actionable follow-up pipeline. Verified live dashboard 200. CLOCK NOTE: PC wall-clock is ~2h AHEAD of Africa/Johannesburg (TZ-joburg 14:27 vs PC 16:27); loop uses plain `date` (=Paul's wall clock) for the 19:00 stop, which is what '7pm' means; watchdog uses TZ-joburg but loop CronDeletes it at PC-19:00 so harmless. Additive, no regression. [C15 ~16:36] LOCK ESTIMATES TRACKER status logic — spec/requests/estimates_tracker_spec.rb (4 examples, 0 failures) parses the Inertia data-page props + asserts status_colour on real records: accepted→green, not-accepted+future-appt→blue, not-accepted+nothing→yellow, linked-COT-all-completed→green. Paul's headline tracker feature now regression-proof. (Reused patient/appointment factories; data-page regex + CGI.unescapeHTML to read props.) Test DB already had source migration from C11. No live deploy (spec only). [C16 ~16:42] TRACKER AGED FOLLOW-UP nudge (non-AI) — EstimatesController#index flags rows aged = outstanding(yellow) & created >30 days ago + stats.aged/aged_value; Estimates.jsx clickable amber banner 'N overdue follow-ups · RY — worth a call' (→ filters to Outstanding) + amber left-border on aged rows. Reception's chase-list. Verified live 200: aged=403 / R6.08m overdue follow-ups. REGRESSION: tracker spec 4/4 still green (additive). Additive, no regression. [C17 ~16:47] TRACKER patient-name LINKS (non-AI UX) — index row now ships patient_id; Estimates.jsx wraps the name in a Link→/patients/:id (one-click from the chase-list to the patient to action a follow-up). Verified live: patient_id in props, /estimates 200; REGRESSION tracker spec 4/4. Additive, no regression. [C18 ~16:51] DASHBOARD 'Outstanding' card now CLICKABLE→/accounts (wrapped in Link, subtitle '→', consistent with the pipeline card). jsx-only, SAFE-deployed (vite before restart). Verified dashboard 200 + /accounts 200. Additive, no regression. [C19 ~16:55] REGRESSION SPECS for newer features (non-AI) — spec/requests/dashboard_pipeline_spec.rb (2: dashboard stats has estimates_pipeline/estimates_awaiting; recent draft counts + value, >90d draft EXCLUDED, accepted excluded) + added aged-flag case to estimates_tracker_spec (>30d outstanding→aged true, recent→false). Combined run 7 examples 0 failures. Locks C14 (dashboard pipeline scoping) + C16 (aged flag). No live deploy (specs only). [C20 ~16:59] GLOBAL SEARCH verified CLEAN (honest 'meets best practice') — SearchController#search_estimates already matches estimate_number + patient first/last/full name (same as invoices); live test patient-name→5 estimates, number→1; GlobalSearch.jsx has proper empty states ('Type at least 2 characters…', 'No results for "q"'). CONCRETE WIN shipped: Estimates.jsx quick-FILTER BOX — client-side filter the 500-row tracker by patient/number/account (combines with the status filter). SAFE-deployed (vite before restart), /estimates 200. Additive, no regression (controller unchanged, tracker spec unaffected). [C21 ~17:06] SURFACE-AI-NOTE persisted (non-AI build, value-capture for when API returns) — migration 20260607170001 add estimates.ai_note (text, additive); ai_compose stores result['notes'].presence; render_inertia passes ai_note; EstimateShow.jsx dismissible amber 'AI note — please confirm: …' banner (Sparkles+X, no-print) when present. Schema:dump+pull → schema.rb 2026_06_07_170001; TEST db migrated. VERIFIED: throwaway estimate w/ ai_note → 'confirm with the dentist' in page props (banner renders), cleaned up; REGRESSION estimate-show/31 + tracker both 200. When API back, real compose notes auto-populate. [C22 ~17:11] MONEY-PATHS regression net CONFIRMED (honest verify) — spec/models/settlement_spec.rb + spec/requests/settlement_actions_spec.rb = 24 examples 0 failures on the CURRENT test DB (post source+ai_note migrations), covering write-off, payment reverse!, receive_payment overflow→credit, refund, admin role-gate + the money-conservation invariant after every op. AND CI ENFORCES them: .github/workflows/ci.yml runs `bundle exec rspec` (full suite) after db:test:prepare + a schema-drift guard (git diff schema.rb). So the financial core is provably regression-proof per PR — recent migrations didn't disturb it. No code change (confirmation cycle). [C23 ~17:16] STATEMENT PDF readability (Perplexity-benchmarked) — benchmark #1 = a prominent patient balance-due callout + payment reference (previously 'Payable' was buried in the 8pt aging row, no ref). Added StatementPdf#balance_due_callout (bold bordered 'BALANCE DUE (amount you pay the practice): R…' box, 15pt, position:right) + payment reference = account_code + EFT instruction + a brand-COMPLIANT medical-aid line ('submit to your aid to claim back where benefits allow' — no direct-billing claim, patient self-claims) + outstanding_due_cents helper; inserted after transactions in render. VERIFIED: runner render %PDF 49177 bytes (was 47739), due=R912.97 sane; live /accounts/662/statement.pdf 200 %PDF. Additive, ruby-only, no regression. [C24 ~17:22] PATIENT MEDICAL-ALERT banner (clinical safety, Perplexity-benchmarked: persistent banner, allergies first) — PatientShow.jsx adds an always-visible red banner ABOVE the tabs 'Medical alert — Allergies:/Conditions:/Meds:' from medical_history (allergies||chronic_conditions||current_medications); upgrades the prior hover-tooltip-only badge. SAFE-deployed (vite before restart). VERIFIED: set test allergy on demo 2985 → 'Penicillin (TEST)' in page props (banner renders client-side), restored to nil. NOTE: most imported patients have empty medical_history so the banner is sparse now — fills in via digital intake. Additive, no regression (patients 200). [C25 ~17:28] INVOICE PDF + RECALLS verified ALREADY CLEAN (honest 'meets best practice') — invoice document_pdf.rb has TOTAL DUE + medical/self claim split + 'submit this invoice to your scheme for reimbursement' + treating-dentist HPCSA (renders 200 %PDF 9622b); recalls page already shows overdue count (red), per-row red date + OVERDUE badge, click-to-call (tel:), patient links, due→contacted→booked pipeline, sorted by due_on. Small consistent WIN: Recalls.jsx overdue rows get a red left-border (scannable as a block, matches tracker aged rows). Verified /recalls 200. Additive, no regression. [C26 ~17:34] LOCK recent patient-facing features with specs (regression net for a now-mature system) — spec/requests/patient_medical_alert_spec.rb (PatientsController#show ships allergies/chronic_conditions/current_medications → the C24 alert banner's source; clinical safety) + spec/services/statement_pdf_spec.rb (StatementPdf.render → valid %PDF incl. the C23 balance-due callout; the PDF Prawn pipeline had ZERO coverage before). Run 2 examples 0 failures. PracticeBillingProfile.current auto-creates so the PDF smoke-test is self-contained. No live deploy (specs only). System now broadly mature — increasingly finding areas already best-practice (honest), favouring regression-locking + only genuinely valuable changes toward the 19:00 summary. [C27 ~17:40] LOCK the C3+C4 money fixes with specs (were live-validated but had NO CI coverage) — spec/models/billing_guards_spec.rb: (C3) Estimate#accept_and_invoice! creates exactly 1 invoice + a 2nd accept raises RecordInvalid (no double-invoice from a race/double-submit); (C4) Invoice.from_course bills a completed item once + a 2nd generate yields 0 lines (no double-billing). Run 2 examples 0 failures. These were genuine financial-correctness fixes — now regression-proof per PR. No live deploy (specs only). [C28 ~17:44] INVOICE-PDF spec (spec/services/document_pdf_spec.rb → DocumentPdf.invoice renders valid %PDF) completes PDF-pipeline coverage (statement+invoice both spec'd). ALL-GREEN CONFIRMATION: ran the session's whole regression net together = 38 examples, 0 failures (document_pdf 1 + statement_pdf 1 + billing_guards 2 + settlement model 16 + settlement_actions 8 + estimates_tracker 5 + dashboard_pipeline 2 + health 2 + patient_medical_alert 1). NB: the FULL `rspec` (no args) includes service specs (ai_service/whatsapp/google) that hit external APIs + timed out >400s — those aren't part of this session's net; the 38 above are the money/feature/PDF/health coverage built/touched this session, all green. No live deploy. [C29 ~17:57 wind-down] VERIFY all healthy — /health green {db_latency_ms 10.5, queue_pending 5, backup_age_hours 9.1 (fresh, <36h)}; data-integrity re-sweep CLEAN on every money/booking invariant EXCEPT the SAME known orphan payment 8686 (R50, already flagged for Paul — NOT a new issue). System mature + regression-proof; winding toward 19:00 final summary. (This cycle ran ~12min due to C28's full-suite timeout + targeted 38-spec run.) [C30 ~18:00 wind-down] Verified GREEN — /up 200, /health ok (backup_age_hours 9.2 fresh), API still capped. No regressions, nothing genuinely valuable to ship (system mature + 38-spec net green). Heartbeat refreshed; continuing light cycles to 19:00. [C31 ~18:03] Verified green (up 200, /health ok, API still capped) — nothing to ship; heartbeat fresh. [C32 ~18:06] Verified green (up 200, /health ok, API still capped) — nothing to ship; heartbeat fresh. [C33 ~18:09] Verified green — nothing to ship; heartbeat fresh. [C34 ~18:12] Verified green — nothing to ship; heartbeat fresh. [C35 ~18:15] Verified green — nothing to ship; heartbeat fresh. [C36 ~18:18] Verified green — nothing to ship; heartbeat fresh. [C37 ~18:21] Verified green — nothing to ship; heartbeat fresh. [C38 ~18:24] Verified green — nothing to ship; heartbeat fresh. [C39 ~18:27] Verified green — nothing to ship; heartbeat fresh. [C40 ~18:30] Verified green — nothing to ship; heartbeat fresh. [C41 ~18:33] Verified green — nothing to ship; heartbeat fresh. [C42 ~18:36] Verified green — nothing to ship; heartbeat fresh. [C43 ~18:39] Verified green — nothing to ship; heartbeat fresh. [C44 ~18:42] Verified green — nothing to ship; heartbeat fresh. [C45 ~18:45] Verified green — nothing to ship; heartbeat fresh. [C46 ~18:48] Verified green — nothing to ship; heartbeat fresh. [C47 ~18:51] Verified green — nothing to ship; heartbeat fresh. [C48 ~18:54] Verified green — nothing to ship; heartbeat fresh. [C49 ~18:57] Verified green (db 0.2ms, queue 5, backup 10.1h) — nothing to ship; heartbeat fresh.

═══════════════════════════════════════════════════════════════════
██ PRO-LOOP FINAL SUMMARY — 2026-06-07, ran ~14:57→19:00 (Paul's clock), 49 cycles, ZERO regressions ██
═══════════════════════════════════════════════════════════════════
Self-improve-loop-pro on the live Ivory PMS (rig http://100.73.38.21:3000). Every shipped change was additive/reversible, verified for real, and regression-gated; guardrails (no Elixir, no login resets, no real WhatsApp/SMS, money-conservation + PHI/POPIA + access-control) held all 49 cycles.

HEADLINE WINS (grouped):
(a) AI ESTIMATE/INVOICE COMPOSER — grounded in the practice's OWN Estimate Assistant recipes (canal table, ICD-10, 190-code catalogue); eval harness script/evals/composer_evals.rb (13 golden cases, baseline 8/8 GREEN); edit-rate capture (estimate_lines.source='ai') as the live feedback signal; ai_note "please confirm" banner; degrades gracefully when the API is capped (no 500s).
(b) ESTIMATES TRACKER suite (Paul's ask) — /estimates is a colour lifecycle tracker (yellow=outstanding / blue=booked / green=accepted-or-COT-done), pipeline value (R6.4m outstanding / R0.94m booked), CSV export, aged>30d follow-up nudge, patient-name links, quick-filter box, dashboard pipeline KPI (90-day scoped), + request-spec locked.
(c) DENTAL CHART tab (Paul's ask) — per-patient odontogram, RED=needs-work / BLACK=done/existing, planned-procedure tooltip; COT page unaffected.
(d) OBSERVABILITY — /health now a complete readiness probe (db up + db_latency_ms + queue_pending + backup_age_hours, degraded/503 thresholds) + request-spec; backup-freshness marker (ops/last_backup.json).
(e) FINANCIAL REGRESSION NET (CI-enforced per PR) — settlement 24 + billing_guards (C3 estimate double-accept → 1 invoice; C4 COT double-bill guard) + money-conservation invariant; full session net = 38 examples 0 failures, run together green.
(f) PATIENT-FACING — statement PDF prominent BALANCE-DUE callout + payment reference + compliant medical-aid claim line (Perplexity-benchmarked); invoice PDF verified already-complete (claim split + HPCSA); persistent MEDICAL-ALERT banner (allergies/conditions/meds, clinical safety); statement + invoice PDFs + medical-alert all spec'd (PDF pipeline went from 0 → covered).
(g) SELF-HEALING WATCHDOG — 10-min cron liveness check restarts a dead loop chain.

OPEN FOR PAUL (2):
1. 🔴 ANTHROPIC_API_KEY is at its usage/spend cap — ALL AI features (composer, likely WhatsApp-AI/voice/intent) are DOWN until the limit is lifted (Anthropic console) or it auto-regains 2026-07-01 00:00 UTC. Lift it → the composer is back instantly; re-run composer_evals.rb to reconfirm 8/8.
2. 🔴 ORPHAN PAYMENT 8686 — R50.00 cash, no invoice/account/patient owner (created 2026-06-06). Real money attached to nothing; attribute it to a patient/account or confirm it's a test row to remove. (The owner-validation guard now blocks new ones.)

FINAL /health (19:00): {status: ok, db: true, db_latency_ms: 0.2, queue_pending: 5, backup_age_hours: 10.2}. Data-integrity: CLEAN on every money/booking invariant except the known orphan 8686. System is mature, green, and regression-proof. Loop ended; watchdog removed.
═══════════════════════════════════════════════════════════════════

## Open items for Paul
- Rig IP **`10.0.0.125` is STATIC** (DHCP-reserved per the Vox network doc) — confirmed by Paul 2026-06-05, so the diary bookmark is permanent. (Wired connection still a nice-to-have over WiFi, not a blocker.)
- Cloudflare WAF skip rule for `/webhooks/*` (needs CF dashboard / a token) — for when the WhatsApp number goes on ads.
- When ready: flip the switch (reception → Ivory diary).
